% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_descriptive_stats.R
\name{summarize_descriptive_stats}
\alias{summarize_descriptive_stats}
\title{Descriptive summary of several features, optionally split by group}
\usage{
summarize_descriptive_stats(data, feats, group = NULL, group_lv = NULL)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per
observation and one column per feature.}

\item{feats}{Character vector of numeric column names in \code{data} to
summarise, in output order.}

\item{group}{Optional grouping vector with one entry per row of \code{data}. When
\code{NULL}, all rows are summarised together and no \code{group} column is
returned.}

\item{group_lv}{Group levels to report, in output order. When \code{NULL}, the
levels present in \code{group} are used: the factor levels if \code{group} is a
factor, the sorted unique values otherwise. Rows belonging to any other
level are dropped.}
}
\value{
A data.frame with one row per feature, or per feature and group
level when \code{group} is supplied. The leading columns are \code{features} and,
when grouped, \code{group}. The remaining columns are:

\describe{
\item{\code{n}, \code{n_missing}}{Finite values the row is based on, and the
values left out for being \code{NA}, \code{NaN} or infinite.}
\item{\code{mean}, \code{sd}, \code{var}, \code{se}, \code{cv}}{Mean, standard deviation,
variance, standard error \code{sd / sqrt(n)} and coefficient of variation
\code{sd / mean}.}
\item{\code{min}, \code{q1}, \code{median}, \code{q3}, \code{max}, \code{iqr}}{Five-number summary and
the interquartile range. Quartiles come from \code{\link[stats:quantile]{stats::quantile()}} with
its default type 7.}
\item{\code{out_lower_bound}, \code{out_upper_bound}}{Tukey outlier fences
\code{q1 - 1.5 * iqr} and \code{q3 + 1.5 * iqr}.}
\item{\code{mad}}{Median absolute deviation from \code{\link[stats:mad]{stats::mad()}}, scaled by
the default 1.4826 so that it estimates \code{sd} for normal data.}
\item{\code{skewness}, \code{excess_kurtosis}}{Shape of the distribution, zero for
a normal sample.}
}
}
\description{
Reduces every feature to one row of sample size, central tendency,
dispersion, quartiles, outlier fences and distribution shape. With a
grouping vector the same row is produced per group level, so the summary
lines up with the tests and plots that compare those levels.
}
\details{
Missing and non-finite values are dropped per feature and per group before
anything is computed, so one \code{Inf} cannot turn a whole row into \code{Inf} or
\code{NaN}; \code{n_missing} records how many were left out. A feature with no finite
value in a group gives an all-\code{NA} row rather than aborting the summary.

\code{skewness} and \code{excess_kurtosis} are the bias-corrected G1 and G2
estimators used by SAS and SPSS, the same quantities as
\code{e1071::skewness(type = 2)} and \code{e1071::kurtosis(type = 2)}. They need three
and four observations respectively, and a non-zero spread, and are \code{NA}
otherwise.

\code{cv} is a ratio, so it only reads as relative dispersion when the values are
positive. On data that crosses zero the mean shrinks towards it and the
ratio explodes without the spread having changed.

\code{out_lower_bound} and \code{out_upper_bound} are the same fences that
\code{\link[=draw_grouped_boxplot]{draw_grouped_boxplot()}} returns as \code{lower_bound} and \code{upper_bound} in
\code{box_summary_stats}. They are where the whiskers may reach, not where they
actually end.
}
\examples{
feats <- c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width")

## All rows together
summarize_descriptive_stats(iris, feats)

## One row per feature and species
by_species <- summarize_descriptive_stats(iris, feats, iris$Species)
by_species[by_species$features == "Petal.Length", ]

## Two of the three species, in a chosen order
summarize_descriptive_stats(iris, "Petal.Length", iris$Species,
                            c("virginica", "versicolor"))

}
\references{
Joanes, D. N. and Gill, C. A. (1998). Comparing measures of sample skewness
and kurtosis. \emph{Journal of the Royal Statistical Society: Series D}, 47(1),
183-189.
}
\seealso{
\code{\link[=draw_grouped_boxplot]{draw_grouped_boxplot()}} to see the same quantities as a plot, and
\code{\link[=compare_two_groups]{compare_two_groups()}} to test the difference between two levels.
}
