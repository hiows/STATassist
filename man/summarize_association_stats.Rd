% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_association_stats.R
\name{summarize_association_stats}
\alias{summarize_association_stats}
\title{Correlation between every pair of features, with all three coefficients}
\usage{
summarize_association_stats(
  data,
  feats = NULL,
  methods = c("pearson", "spearman", "kendall"),
  adj_type = "BH",
  use = c("pairwise.complete.obs", "complete.obs")
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per observation
and one column per feature.}

\item{feats}{Character vector of numeric column names in \code{data} to correlate,
in output order, or \code{NULL} for every numeric column of \code{data}. At least two
are needed.}

\item{methods}{Which coefficients to compute, drawn from \code{"pearson"},
\code{"spearman"} and \code{"kendall"}. Each one named gets a slot of the result, in
the order given; the ones not named have no slot at all.}

\item{adj_type}{Multiplicity adjustment applied across the pairs, any method
from \link[stats:p.adjust.methods]{stats::p.adjust.methods}.}

\item{use}{How a missing value is handled. \code{"pairwise.complete.obs"} reads
each pair on the observations that pair shares, and \code{"complete.obs"} drops
any row with a missing value in any feature first, so that every pair is
read on one set of rows.}
}
\value{
A plain list. One slot per entry of \code{methods}, named after it and
holding four features-by-features matrices with the same dimnames:

\describe{
\item{\code{corr}}{The coefficient, from \code{\link[stats:cor]{stats::cor()}}.}
\item{\code{pvalue}}{The two-sided p-value from \code{\link[stats:cor.test]{stats::cor.test()}}.}
\item{\code{adj_pvalue}}{\code{pvalue} adjusted by \code{adj_type} across the pairs.}
\item{\code{n}}{Observations the pair shared.}
}

Beside those, \code{design} records \code{feats}, \code{n_obs}, \code{methods}, \code{adj_type} and
\code{use}.
}
\description{
Reduces a set of features to the association between each pair of them, as a
square matrix per quantity: the coefficient, its p-value, the p-value adjusted
across the pairs, and how many observations the pair shared. Pearson,
Spearman and Kendall are reported side by side on the same pairs, the way the
comparison functions report a parametric, a rank-based and a robust test side
by side, so that a linear coefficient and a monotonic one disagreeing is
visible rather than a matter of which call was made.
}
\details{
This is a screen rather than a test of one hypothesis. It is the companion of
\code{\link[=summarize_descriptive_stats]{summarize_descriptive_stats()}}, which reduces one feature at a time to a row
of its own; this reduces a pair at a time. Neither returns a \code{sa_comparison}.

Every matrix is symmetric. The upper triangle is computed and mirrored, so a
pair is tested once rather than twice.

The diagonal is a property of the matrix rather than an estimate: \code{corr} is 1
and \code{pvalue} and \code{adj_pvalue} are \code{NA}, since a feature is not tested against
itself. \code{n} on the diagonal is the number of observations that feature has.

The adjustment runs over the pairs that produced a p-value. A pair
\code{\link[stats:cor.test]{stats::cor.test()}} refused, having fewer than three shared observations or no
variance on one side, is not a test that was performed, and counting it into
the family would shrink the other pairs for a comparison that never happened.
Such a pair comes back \code{NA} in all three of \code{corr}, \code{pvalue} and \code{adj_pvalue}
rather than aborting the screen, and features with no variance at all are
named in a message.

Missing and non-finite values are treated alike, an \code{Inf} being as much "no
value to correlate" as an \code{NA}, and \code{n} counts what was left.

The cost is one \code{\link[stats:cor.test]{stats::cor.test()}} per pair per method: thirty features are
435 pairs, and 1305 tests with all three methods asked for. Kendall's is the
slowest of the three, so naming \code{methods} is worth doing on a wide frame.
}
\examples{
feats <- c("mpg", "disp", "hp", "wt")

## All three coefficients on the same pairs
res <- summarize_association_stats(mtcars, feats)
round(res$pearson$corr, 3)
round(res$pearson$adj_pvalue, 4)

## A linear coefficient and a monotonic one on the same pair
c(pearson  = res$pearson$corr["mpg", "disp"],
  spearman = res$spearman$corr["mpg", "disp"])

## One method only
rho <- summarize_association_stats(mtcars, feats, methods = "spearman")
round(rho$spearman$corr, 3)

}
\seealso{
\code{\link[=draw_corrplot]{draw_corrplot()}} to draw a method's \code{corr} with the non-significant
cells left blank, and \code{\link[=summarize_descriptive_stats]{summarize_descriptive_stats()}} for the one-feature-at-a-time
summary this is the pairwise counterpart of.
}
