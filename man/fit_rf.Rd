% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rf.R
\name{fit_rf}
\alias{fit_rf}
\title{Fit a random forest}
\usage{
fit_rf(
  data,
  outcome,
  predictors = NULL,
  outcome_lv = NULL,
  control_label = outcome_lv[1],
  mtry = NULL,
  ntree = 500,
  nodesize = NULL,
  cv = TRUE,
  cv_method = c("repeated_kfold", "kfold", "loocv"),
  n_fold = 5,
  n_repeat = 5,
  seed = NULL
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per observation.
Typically the training half of a \code{\link[=split_data]{split_data()}} result.}

\item{outcome}{The outcome, either the name of a column of \code{data} or a vector
with one entry per row. A numeric outcome is fitted as a regression, and a
factor, character or logical one as a two-class classification.}

\item{predictors}{Column names to fit on, or \code{NULL} for every column of \code{data}
except the outcome. Numeric, logical, factor and character columns are
accepted; a column that takes a single value is left out with a message,
since it cannot contribute.}

\item{outcome_lv}{The two classes, reference first, so that the reported
sensitivity and the predicted probability are about the second one. Supplying
it also states that the outcome is to be classified, which is the one thing a
numeric column holding two values cannot say on its own. \code{NULL} sorts the
classes of an outcome that is not numeric, and leaves a numeric one to the
regression.}

\item{control_label}{The reference class on its own, for when the other one
needs no saying. Defaults to \code{outcome_lv[1]}, so a call that names one of the
two names the reference either way; naming both and disagreeing is an error.}

\item{mtry}{Predictors offered at each split, one value to score or several to
choose between. \code{NULL} is the rule of thumb: \code{floor(sqrt(p))} for a
classification and \code{floor(p / 3)} for a regression, at least 1. A value above
the predictor count is refused rather than quietly reset by the engine.}

\item{ntree}{Trees to grow. More is steadier rather than more prone to
overfitting, and costs time.}

\item{nodesize}{Smallest number of rows a leaf may hold, so larger grows
shallower trees. \code{NULL} is \code{randomForest}'s own default: 1 for a
classification and 5 for a regression.}

\item{cv}{Whether to cross-validate. \code{FALSE} grows the single candidate the
grid names, once, and reports no resampled performance. The out-of-bag
\code{fit_stats} are there either way.}

\item{cv_method}{Resampling scheme: \code{"repeated_kfold"} for \code{n_repeat} runs of
\code{n_fold}-fold cross-validation, \code{"kfold"} for one, or \code{"loocv"} for
leave-one-out.}

\item{n_fold}{Folds per run, used by \code{"repeated_kfold"} and \code{"kfold"}.}

\item{n_repeat}{Number of runs, used by \code{"repeated_kfold"}.}

\item{seed}{Seed for the forest and the fold assignment, or \code{NULL} to use the
stream as it stands. Supplying one does not disturb the caller: the previous
random number state is put back when the function returns.}
}
\value{
An object of class \code{sa_model}, the same eleven elements
\code{\link[=fit_linear_regression]{fit_linear_regression()}} returns, with these differences:

\describe{
\item{\code{analysis}}{\code{"random_forest"}.}
\item{\code{terms}}{The predictors, in descending order of importance, since a
forest expands no factor into dummy terms.}
\item{\code{design}}{Holds \code{outcome_lv}, \code{n_events} and \code{event_rate} for a
two-class outcome, as \code{\link[=fit_logistic_regression]{fit_logistic_regression()}} does, and neither for a
continuous one.}
\item{\code{parameters}}{Holds the \code{mtry} that was fitted rather than the grid
that was searched, \code{ntree}, \code{nodesize}, and \code{n_candidates}, how many
values of \code{mtry} were scored. The grid itself is the rows of
\code{performance}.}
\item{\code{coefficients}}{\code{estimate}, the permutation importance, and
\code{impurity}, the impurity-based one. There is no intercept row, no
\code{odds_ratio} and no inference column.}
\item{\code{fit_stats}}{Measured on the out-of-bag predictions: \code{oob_r_squared},
\code{oob_rmse} and \code{oob_mae} for a regression, and \code{oob_accuracy},
\code{oob_error}, \code{oob_kappa}, \code{oob_sensitivity} and \code{oob_specificity} for a
classification, each with \code{n_oob}, the rows that were out of bag of at
least one tree and so could be predicted.}
\item{\code{performance}}{One row per value of \code{mtry}, the chosen one being the
row that matches \code{parameters$mtry}, or \code{NULL} when \code{cv = FALSE}.}
}
}
\description{
Grows a forest of regression or classification trees on a set of predictors,
and scores it by cross-validation on the data it was fitted to. Which outcome
it is decides which forest is grown: a numeric outcome is a regression forest
and a two-class one a classification forest, as in \code{\link[=fit_elastic_net]{fit_elastic_net()}}, so one
function covers what \code{\link[=fit_linear_regression]{fit_linear_regression()}} and \code{\link[=fit_logistic_regression]{fit_logistic_regression()}}
cover between them.
}
\details{
What comes back in place of a coefficient table is an importance table: one row
per predictor, in descending order of how much the forest lost when that
predictor was shuffled. It says which predictors carried the fit and not which
way they pushed it, since a forest is free to use the same predictor in opposite
directions in different regions of the data.

\describe{
\item{\code{estimate} is permutation importance, and there is no standard error
beside it}{Each tree is scored on its out-of-bag rows, then scored again
with one predictor's values shuffled among them, and \code{estimate} is the mean
loss over the trees: \verb{\%IncMSE} for a regression and
\code{MeanDecreaseAccuracy} for a classification. \code{impurity} is the other
measure the same fit reports, the total drop in node impurity that
predictor was responsible for — \code{IncNodePurity} or \code{MeanDecreaseGini}.
Neither is an estimate of anything a distribution is defined over, so the
inference columns are absent from the table rather than present and \code{NA},
exactly as they are for \code{\link[=fit_elastic_net]{fit_elastic_net()}}, and
\code{is.null(fit$coefficients$pval)} tells the two kinds of table apart.
The values are the unscaled ones. \code{randomForest::importance()} divides them
by their standard deviation across trees by default, which makes a t-shaped
ratio that is not referred to any distribution, so what is reported is the
mean loss itself, on the scale of the metric it was measured in.}
\item{A negative importance is a value, not a gap}{Shuffling a predictor that
carried nothing can leave the forest very slightly better than it was, and
the mean loss then comes out below zero. It reads as it should: this
predictor did no better than its own permutation.}
\item{The terms are the predictors}{A tree splits a factor on its levels
directly, so nothing is dummy coded and a \code{k}-level factor is one term
rather than \code{k - 1}. This is the one model here where \code{terms} and
\code{design$predictors} hold the same names — in a different order, since the
table is sorted by importance and the design records the order the columns
were read in.}
\item{\code{fit_stats} is measured out of bag}{Every tree is grown on a bootstrap
sample of the rows, which leaves the rest of them out of bag for that tree,
and \code{randomForest} predicts each row from the trees that did not see it.
That is already a held-out score, and it is the honest one: a forest asked
to predict the rows it was fitted to reports something close to perfect
whatever the data held. The \code{oob_} prefix says these numbers are not the
in-sample kind the other models report.}
\item{Cross-validation chooses \code{mtry} when there is more than one to choose
from}{\code{mtry} is the only argument \code{\link[caret:train]{caret::train()}} tunes for a forest;
\code{ntree} and \code{nodesize} are the same for every candidate. A single value is
scored rather than chosen, as in \code{\link[=fit_linear_regression]{fit_linear_regression()}}, and a vector is
compared the way \code{\link[=fit_elastic_net]{fit_elastic_net()}} compares penalties, \code{parameters$mtry}
then holding the value that won and \code{performance} one row per candidate.
Unlike a penalty, \code{mtry} has a default worth fitting, so \code{NULL} resolves to
the rule of thumb — the square root of the predictor count for a
classification, a third of it for a regression — rather than to a grid, and
\code{cv = FALSE} needs nothing added to work.}
}

Rows with a missing value in the outcome or in any predictor are dropped before
the folds are drawn rather than inside each fit, and \code{design$n_dropped} reports
how many went. A predictor that takes a single value is left out with a message.

One predictor is enough. There is no budget to divide as there is under a
penalty, so \code{predictors = "wt"} grows a forest of stumps on that one column
rather than raising the error \code{\link[=fit_elastic_net]{fit_elastic_net()}} raises.

For a two-class outcome the direction is \code{outcome_lv}, read as
\code{\link[=fit_logistic_regression]{fit_logistic_regression()}} reads it: the first level is the reference, so
\code{oob_sensitivity} is the share of \code{outcome_lv[2]} the forest found,
\code{oob_specificity} the share of \code{outcome_lv[1]} it left alone, and
\code{predict(model, newdata, type = "response")} is the probability of
\code{outcome_lv[2]}. The importance table itself has no direction to report, which
is why there is no \code{odds_ratio} column beside it. \code{control_label} says the
same thing with one name instead of two and says it alone, so it too is enough
to make a column of zeroes and ones a classification. Naming both and pointing
them at different classes is an error rather than a re-pointing, since an
\code{outcome_lv} holds the two classes and nothing else; the comparison functions
read the argument the other way, as \code{\link[=compare_two_groups]{compare_two_groups()}} describes.

A forest is random beyond the folds — the rows of each tree and the predictors
of each split are both draws — so \code{seed} fixes more here than it does in the
other models, where it fixes only the fold assignment. Two calls without one
give slightly different importance values on the same data.
}
\examples{
## A single forest without resampling (fast enough for examples).
fit <- fit_rf(mtcars, outcome = "mpg", ntree = 50, cv = FALSE, seed = 1)
fit$coefficients
fit$fit_stats

\donttest{
## Cross-validated fit and an `mtry` search.
fit_rf(mtcars, outcome = "mpg", ntree = 200,
       cv_method = "kfold", n_fold = 5, seed = 1)
tuned <- fit_rf(mtcars, outcome = "mpg", mtry = c(2, 5, 10), ntree = 200,
                cv_method = "kfold", n_fold = 5, seed = 1)
tuned$parameters[c("mtry", "n_candidates")]

## Two-class outcome and a simulated regression scored against known truth.
iris2 <- iris[iris$Species != "setosa", ]
clf <- fit_rf(iris2, outcome = "Species",
              outcome_lv = c("versicolor", "virginica"),
              ntree = 200, cv = FALSE)
clf$fit_stats[c("oob_accuracy", "oob_sensitivity", "oob_specificity")]
sim <- simulate_regression(seed = 1)
rf <- do.call(fit_rf, c(sim$args, list(ntree = 200, cv = FALSE)))
scored <- merge(rf$coefficients, sim$truth_term, by = "terms")
tapply(scored$estimate, scored$beta != 0, mean)
}

}
\seealso{
\code{\link[=fit_linear_regression]{fit_linear_regression()}}, \code{\link[=fit_logistic_regression]{fit_logistic_regression()}} and
\code{\link[=fit_elastic_net]{fit_elastic_net()}} for the models that do report an effect per predictor,
\code{\link[=split_data]{split_data()}}, which defines the rows this is fitted on, \code{\link[=coef.sa_model]{coef.sa_model()}}
for the importance table, and \code{\link[=predict.sa_model]{predict.sa_model()}} for predicting the rows it
was not fitted on.
}
