% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical.R
\name{as.table.sa_categorical}
\alias{as.table.sa_categorical}
\title{The contingency table a categorical comparison was run on}
\usage{
\method{as.table}{sa_categorical}(x, ...)
}
\arguments{
\item{x}{A categorical comparison, as returned by
\code{\link[=compare_categorical_groups]{compare_categorical_groups()}}.}

\item{...}{Ignored, present for consistency with \code{\link[=as.table]{as.table()}}.}
}
\value{
A two-dimensional \code{\link[=table]{table()}} of counts, its dimnames named after the
two axes of the design.
}
\description{
\verb{$cells} is the canonical form, one row per cell, because that is the shape
that survives being written out as JSON with its labels attached. A table is
the shape to read it in, so it is built on request rather than stored twice
and left to drift.
}
\details{
The rows dropped for a missing value or for a level outside \code{category_lv} are
already gone, so this is the table the tests were run on rather than a fresh
\code{\link[=table]{table()}} of the input.
}
\examples{
res <- compare_categorical_groups(
  data.frame(smoker = rep(c("y", "n"), each = 30),
             grade  = c(rep(c("high", "low"), c(22, 8)),
                        rep(c("high", "low"), c(9, 21))))
)
as.table(res)

## Which is the table the tests were run on, so it feeds `stats` directly.
stats::chisq.test(as.table(res))

}
