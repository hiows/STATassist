% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_forest_plot.R
\name{draw_forest_plot}
\alias{draw_forest_plot}
\alias{plot.sa_comparison}
\title{Draw a forest plot of a comparison result}
\usage{
draw_forest_plot(
  comparison_result,
  test = names(comparison_result$tests)[1],
  type = c("auto", "estimate", "posthoc", "pvalue"),
  feats = NULL,
  use_adjusted = TRUE,
  alpha = 0.05,
  sort_by = c("none", "pvalue"),
  dark = FALSE,
  xlim = NULL,
  xlab = NULL,
  main = NULL,
  col_signif = "#D1495B",
  col_plain = "#7F8C8D",
  cex.axis = 0.9,
  cex.lab = 1.1,
  cex.main = 1.2,
  cex.legend = 1,
  ...
)

\method{plot}{sa_comparison}(x, ...)
}
\arguments{
\item{comparison_result}{A comparison result, as returned by
\code{\link[=compare_two_groups]{compare_two_groups()}}, \code{\link[=compare_multiple_groups]{compare_multiple_groups()}} or
\code{\link[=compare_one_sample]{compare_one_sample()}}.}

\item{test}{Which test to draw. One of \code{names(comparison_result$tests)}.}

\item{type}{\code{"auto"}, \code{"estimate"}, \code{"posthoc"} or \code{"pvalue"}.}

\item{feats}{Character vector of features to draw, in display order from the
top of the plot down. \code{NULL}, the default, draws every feature of the
chosen table, except in the post-hoc view, where it draws the first feature
of the post-hoc table. A feature that has no contrasts because its omnibus
test did not qualify is reported in a \code{message()} and left out.}

\item{use_adjusted}{Logical. If \code{TRUE}, read the \code{pval_adj} column; if
\code{FALSE}, the unadjusted \code{pval}. The colouring, the sorting, the p-value
view and the labels all follow, so the plot always describes the p-value it
actually used.}

\item{alpha}{Threshold marked on the p-value view and used to colour the
points of the estimate view.}

\item{sort_by}{\code{"none"} to keep the feature order of the result, or
\code{"pvalue"} to draw the most significant rows at the top.}

\item{dark}{If \code{TRUE}, use a dark background with light text.}

\item{xlim}{Numeric length-2 x axis range, or \code{NULL} to derive it from the
values being drawn. A supplied range is used as given, and the interval
bounds are clamped to it, so an interval running past the range still
reaches the edge of the panel instead of vanishing.}

\item{xlab, main}{Axis and title labels. Both are derived from the result when
left \code{NULL}.}

\item{col_signif, col_plain}{Colours for rows at or below \code{alpha} and for the
rest.}

\item{cex.axis, cex.lab, cex.main, cex.legend}{Character expansion for the axis
annotation, the axis label, the title and the legend.}

\item{...}{Arguments the \code{plot()} method passes on. Anything
\code{draw_forest_plot()} does not name is ignored.}

\item{x}{The same comparison result, under the name the \code{\link[=plot]{plot()}} generic
fixes for its first argument.}
}
\value{
The plotted data.frame, invisibly, in the row order it was drawn,
with the view that \code{type = "auto"} resolved to attached as the attribute
\code{"view"}.
}
\description{
Draws the estimate of each feature beside its confidence interval, falling
back to a bar of \code{-log10(pval_adj)} for a table that has no interval to
draw, which is every omnibus test.
}
\details{
Only the columns the result contract guarantees are read, which is why one
function covers every scenario: it never asks whether the object came from
two groups, three groups or a single sample, only whether the table it was
handed has intervals or p-values, and every table has one or the other.
\code{plot()} on an \code{sa_comparison} is the same function under the name R users
reach for first.

Three views are available:

\describe{
\item{\code{"estimate"}}{A forest plot of the effect estimate and its
confidence interval, one row per feature. Available whenever the chosen
table has finite intervals, which the two-group and one-sample scenarios
always do.}
\item{\code{"posthoc"}}{The same forest plot for the pairwise contrasts of a
multi-group comparison, one row per contrast. Only the first feature of
the post-hoc table is drawn unless \code{feats} names the ones to draw. An
estimate reads as \code{group1 - group2}, the direction the row label spells
out, and the reference level is the one subtracted, so a point to the
right of the guide agrees in sign with the \code{log2fc} a volcano plot of the
same comparison draws.}
\item{\code{"pvalue"}}{\code{-log10()} of the p-value per feature with the \code{alpha}
threshold marked. The fallback when a table has no interval to draw,
which is the case for every omnibus test.}
}

\code{type = "auto"}, the default, picks the first of those three that the chosen
table can actually support.

The function changes graphical parameters and the panel layout, and restores
both on exit, so the caller's device is left as it was found. The legend
lives in a narrow panel of its own on the right, where it cannot cover the
rows it describes.

The estimate view marks the null value with a vertical line, at zero for a
difference and at one for the Brunner-Munzel relative effect, which is the
one quantity in the package whose null is not zero.
}
\examples{
iris2 <- iris[iris$Species != "setosa", ]
feats <- c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width")
res <- compare_two_groups(iris2, feats, iris2$Species,
                          c("virginica", "versicolor"))

## Mean differences with their intervals
draw_forest_plot(res)

## plot() reaches the same function
plot(res)

## The same comparison read through the rank-based test
draw_forest_plot(res, test = "wilcox_test", sort_by = "pvalue")

## Two features only, drawn in the order they are named, against the
## unadjusted p-value
draw_forest_plot(res, feats = c("Petal.Width", "Petal.Length"),
                 use_adjusted = FALSE)

## An axis range of your own, so that two plots can be read against each other
draw_forest_plot(res, xlim = c(0, 2))

## A multi-group omnibus table has no interval, so "auto" falls through to
## the pairwise contrasts.
multi <- compare_multiple_groups(iris, feats, iris$Species,
                                 levels(iris$Species))
draw_forest_plot(multi, feats = "Petal.Length")

## Several features at once label each contrast with the feature it belongs to
draw_forest_plot(multi, feats = c("Petal.Length", "Sepal.Width"))
draw_forest_plot(multi, type = "pvalue")

}
\seealso{
\code{\link[=draw_volcano_plot]{draw_volcano_plot()}}, which plots effect size against significance
rather than estimate against interval, and \code{\link[=draw_grouped_boxplot]{draw_grouped_boxplot()}} for the
input data.
}
