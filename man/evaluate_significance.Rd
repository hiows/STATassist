% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_significance.R
\name{evaluate_significance}
\alias{evaluate_significance}
\title{Combine fold changes and p-values into a significance table}
\usage{
evaluate_significance(
  feats,
  fold_change,
  pvalue,
  adj_type = "BH",
  log2fc_cutoff = 1,
  pval_cutoff = 0.05
)
}
\arguments{
\item{feats}{Character vector of feature names. One output row per entry, in
this order.}

\item{fold_change}{Numeric vector of fold changes, one per feature, such as
the output of \code{\link[=calculate_fold_change]{calculate_fold_change()}}. Reported as \code{log2fc}.}

\item{pvalue}{Numeric vector of unadjusted p-values, one per feature.}

\item{adj_type}{Multiplicity adjustment passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}, one of
\code{stats::p.adjust.methods}. Use \code{"none"} to leave the p-values alone.}

\item{log2fc_cutoff}{Minimum \code{abs(log2fc)} required to call a feature
significant. The default of 1 is a two-fold change.}

\item{pval_cutoff}{Largest \code{adj_pvalue} allowed for a feature to be called
significant.}
}
\value{
A data.frame with one row per feature and the columns \code{features},
\code{log2fc}, \code{pvalue}, \code{adj_pvalue} and \code{is_signif}.
}
\description{
Puts the two axes of a volcano plot side by side, adjusts the p-values for
multiplicity and flags the features that clear both cutoffs.
}
\details{
\code{fold_change} and \code{pvalue} must have one value per feature. When either
carries names, as \code{\link[=calculate_fold_change]{calculate_fold_change()}} output does, the names must match
\code{feats} and are used to reorder the values; otherwise position is trusted.

\code{is_signif} combines \code{abs(log2fc) >= log2fc_cutoff} with
\code{adj_pvalue <= pval_cutoff}, and is therefore judged on the adjusted
p-values. Pass \code{adj_type = "none"} to test the raw ones.

The two ways a fold change can fall outside the domain of \code{log2()} are not
equivalent. A fold change of exactly zero gives \code{log2fc = -Inf}, an
infinitely large decrease, which clears any magnitude cutoff. A negative fold
change gives \code{log2fc = NaN}, which makes \code{is_signif} \code{NA}: such a feature is
undecided rather than decided against. Both are listed in a \code{message()}.
Note that \code{subset()} and \code{[} drop \code{NA} rows silently, so filter with
\code{which(x$is_signif)} if the count matters.
}
\examples{
iris2 <- iris[iris$Species != "setosa", ]
feats <- c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width")
lv <- c("versicolor", "virginica")

fc <- calculate_fold_change(iris2, feats, iris2$Species, lv,
                            case_label = "virginica")
pv <- compare_two_groups(iris2, feats, iris2$Species,
                         lv)$test_results$t_test$pval

evaluate_significance(feats, fc, pv)

## A stricter fold change cutoff leaves nothing significant here, since the
## two species are well under two-fold apart on every measurement.
evaluate_significance(feats, fc, pv, log2fc_cutoff = 1.5)

## Bonferroni instead of the default Benjamini-Hochberg
evaluate_significance(feats, fc, pv, adj_type = "bonferroni",
                      log2fc_cutoff = 0.1)

}
\seealso{
\code{\link[=calculate_fold_change]{calculate_fold_change()}} for the \code{fold_change} argument,
\code{\link[=compare_two_groups]{compare_two_groups()}} for \code{pvalue}, and \code{\link[=draw_volcano_plot]{draw_volcano_plot()}} to plot the
result.
}
