% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_significance.R
\name{estimate_significance}
\alias{estimate_significance}
\title{Reduce a comparison to one significance verdict per feature}
\usage{
estimate_significance(
  res,
  test = "t_test",
  log2fc_cutoff = 1,
  pval_cutoff = 0.05,
  adj_type = NULL
)
}
\arguments{
\item{res}{A comparison result, as returned by \code{\link[=compare_two_groups]{compare_two_groups()}}.}

\item{test}{Which test in \code{res} supplies the p-values. One of
\code{names(res$tests)}, so \code{"t_test"}, \code{"wilcox_test"} or \code{"robust_test"} for a
two-group comparison.}

\item{log2fc_cutoff}{Minimum \code{abs(log2fc)} required to call a feature
significant. The default of 1 is a two-fold change.}

\item{pval_cutoff}{Largest \code{adj_pvalue} allowed for a feature to be called
significant.}

\item{adj_type}{Multiplicity adjustment. \code{NULL}, the default, reuses the
\code{pval_adj} column that \code{\link[=compare_two_groups]{compare_two_groups()}} already computed. Naming a
method from \link[stats:p.adjust.methods]{stats::p.adjust.methods} re-adjusts the raw p-values instead,
which is the only way to get a different adjustment without rerunning the
comparison. Passing a method here does not adjust twice: the input is always
the unadjusted \code{pval} column.}
}
\value{
A data.frame with one row per feature and the columns \code{features},
\code{log2fc}, \code{pvalue}, \code{adj_pvalue} and \code{is_signif}. The cutoffs, the test name
and the adjustment actually used are attached as attributes, which is where
\code{\link[=draw_volcano_plot]{draw_volcano_plot()}} picks them up so that the plotted guides cannot
disagree with the verdict.
}
\description{
Puts the two axes of a volcano plot side by side and flags the features that
clear both cutoffs. The effect size comes from the comparison's \code{effect}
table and the p-value from whichever test is named, so a single comparison can
be read out through the parametric, the rank-based or the robust lens without
recomputing anything.
}
\details{
\code{is_signif} combines \code{abs(log2fc) >= log2fc_cutoff} with
\code{adj_pvalue <= pval_cutoff}, and is therefore judged on the adjusted
p-values. Pass \code{adj_type = "none"} to test the raw ones.

The two ways a fold change can fall outside the domain of \code{log2()} are not
equivalent. A fold change of exactly zero gives \code{log2fc = -Inf}, an
infinitely large decrease, which clears any magnitude cutoff. A fold change
whose two group centres have opposite signs gives \code{log2fc = NaN}, which makes
\code{is_signif} \code{NA}: such a feature is undecided rather than decided against.
\code{\link[=compare_two_groups]{compare_two_groups()}} reports both kinds when it builds the \code{effect} table.
Note that \code{subset()} and \code{[} drop \code{NA} rows silently, so filter with
\code{which(x$is_signif)} if the count matters.
}
\examples{
iris2 <- iris[iris$Species != "setosa", ]
res <- compare_two_groups(
  data     = iris2,
  feats    = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width"),
  group    = iris2$Species,
  group_lv = c("virginica", "versicolor")
)

estimate_significance(res)

## The same comparison judged on the rank-based and the robust test
estimate_significance(res, test = "wilcox_test", log2fc_cutoff = 0.1)
estimate_significance(res, test = "robust_test", log2fc_cutoff = 0.1)

## A stricter fold change cutoff leaves nothing significant here, since the
## two species are well under two-fold apart on every measurement.
estimate_significance(res, log2fc_cutoff = 1.5)

## Bonferroni instead of the Benjamini-Hochberg the comparison used
estimate_significance(res, adj_type = "bonferroni", log2fc_cutoff = 0.1)

}
\seealso{
\code{\link[=compare_two_groups]{compare_two_groups()}} for the input and \code{\link[=draw_volcano_plot]{draw_volcano_plot()}} for
the output.
}
