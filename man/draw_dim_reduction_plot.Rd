% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_dim_reduction_plot.R
\name{draw_dim_reduction_plot}
\alias{draw_dim_reduction_plot}
\alias{plot.sa_reduction}
\title{Draw a reduction as a scatter of its points}
\usage{
draw_dim_reduction_plot(
  reduction_result,
  group = NULL,
  group_lv = NULL,
  cluster_result = NULL,
  cluster_lv = NULL,
  dims = c(1L, 2L),
  anno_points = FALSE,
  dark = FALSE,
  asp = NULL,
  col = NULL,
  pch = NULL,
  cex = 1.2,
  xlim = NULL,
  ylim = NULL,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  cex.axis = 1.2,
  cex.lab = 1.3,
  cex.main = 1.3,
  cex.legend = 1.1,
  cex.anno = NULL
)

\method{plot}{sa_reduction}(x, ...)
}
\arguments{
\item{reduction_result}{A reduction, as returned by \code{\link[=perform_pca]{perform_pca()}},
\code{\link[=perform_tsne]{perform_tsne()}} or \code{\link[=perform_umap]{perform_umap()}}.}

\item{group}{One label per point, or \code{NULL} for no shape. Points are the rows
the reduction kept, which \code{reduction_result$points} names, and on the
feature scale they are features rather than samples.}

\item{group_lv}{The levels of \code{group} in the order they are drawn and listed
in, or \code{NULL} for the factor's own order and otherwise alphabetical. Unlike
\code{\link[=draw_grouped_boxplot]{draw_grouped_boxplot()}}'s, this argument selects no rows: a level \code{group}
uses and \code{group_lv} leaves out is an error rather than a point dropped from
the picture.}

\item{cluster_result}{A clustering of the same points, as returned by
\code{\link[=cluster_hclust]{cluster_hclust()}}, \code{\link[=cluster_kmeans]{cluster_kmeans()}}, \code{\link[=cluster_dbscan]{cluster_dbscan()}} or
\code{\link[=cluster_snn]{cluster_snn()}}, or \code{NULL} for no colouring.}

\item{cluster_lv}{One label per cluster, in the order the clustering numbers
them, or \code{NULL} for \verb{#1}, \verb{#2}, and so on. Noise is still listed as
\code{noise (n)} when present.}

\item{dims}{Which two coordinates to draw, as positions in the score table.}

\item{anno_points}{Whether to write each point's label beside it.}

\item{dark}{Whether to draw on a dark background.}

\item{asp}{Aspect ratio passed to \code{\link[graphics:plot]{graphics::plot()}}, or \code{NULL} to let the
axes fill the panel independently. \code{1} makes distances comparable between
the two axes.}

\item{col}{One colour for every point, one per cluster when \code{cluster_result}
is given, one per group level when only \code{group} is given, or \code{NULL} for
\code{hcl.colors(n, "Dark 2")} on clusters and the foreground colour elsewhere.
Noise is grey whatever this says.}

\item{pch}{One shape for every point, one per group level when \code{group} is
given, or \code{NULL} for \code{16} and the default shape sequence respectively.}

\item{cex}{Size of the plotted points.}

\item{xlim, ylim}{Axis ranges, or \code{NULL} to take them from the coordinates.}

\item{xlab, ylab, main}{Axis and figure labels. \code{NULL} builds them from the
reduction.}

\item{cex.axis, cex.lab, cex.main, cex.legend, cex.anno}{Relative text sizes.
\code{cex.anno} sizes the point labels and \code{NULL} matches \code{cex.legend}.}

\item{x}{A reduction, as returned by \code{\link[=perform_pca]{perform_pca()}}, \code{\link[=perform_tsne]{perform_tsne()}} or
\code{\link[=perform_umap]{perform_umap()}}.}

\item{...}{Arguments passed on to \code{\link[=draw_dim_reduction_plot]{draw_dim_reduction_plot()}}.}
}
\value{
A data.frame of the points as they were drawn, invisibly: \code{points},
\code{x}, \code{y}, \code{col}, \code{pch}, and \code{cluster} and \code{group} when those were given. The
resolved colouring is carried as a \code{"view"} attribute, one of \code{"both"},
\code{"cluster"}, \code{"group"} or \code{"plain"}.
}
\description{
Plots two coordinates of a \code{\link[=perform_pca]{perform_pca()}}, \code{\link[=perform_tsne]{perform_tsne()}} or
\code{\link[=perform_umap]{perform_umap()}} result against each other. A clustering of the same frame
colours the points and a known grouping shapes them, so what the data was
found to say and what the caller already knew are read off one picture.
}
\section{Colour and shape say two different things}{
\code{cluster_result} takes the colours and \code{group} takes the shapes when both are
given, and giving both is the point of the arrangement rather than a conflict
to be resolved. With one channel alone, that channel takes the colours when
\code{col} is named; otherwise a lone clustering is coloured and a lone grouping
is shaped. The question a clustering usually raises is whether it recovered a
grouping that was known all along, and on two channels that is read directly:
one colour per shape is a clustering that found the groups, and a shape split
across colours is a group the data does not see as one thing. Give neither
and the points are drawn in the foreground colour.

The clustering has to be of the same points, which the two contracts already
promise: \code{sa_cluster} and \code{sa_reduction} both read their input through the
same function, so a mismatch is refused rather than lined up by position.
}

\section{Noise}{
\code{\link[=cluster_dbscan]{cluster_dbscan()}} and \code{\link[=cluster_snn]{cluster_snn()}} can leave a point in no cluster at all.
Those points are grey rather than a palette colour, since a point left out is
the absence of a cluster and not a cluster of its own, and the legend counts
them on a line of their own.
}

\section{The axes}{
A principal component analysis reports what share of the variance each of its
components carries, so its axis labels carry it too: \verb{PC1 (30.2\%)} is read
from \verb{$variance} rather than recomputed. An embedding has no such number and
its axes are labelled with their names alone. \code{asp = 1} is what makes one unit
of the vertical axis the same length as one unit of the horizontal, which is
worth setting when the distance between two points is what is being read.
}

\examples{
res <- perform_pca(iris[1:4])

## What was known all along, as shapes. Name `col` to colour the levels
## instead.
draw_dim_reduction_plot(res, group = iris$Species)

draw_dim_reduction_plot(res, group = iris$Species,
                      col = c("#E69F00", "#56B4E9", "#009E73"))

## What the data was found to say, as colours. The clustering was never shown
## the species, so one colour per shape is its own finding.
cl <- cluster_kmeans(iris[1:4], n_clust = 3, seed = 1)
drawn <- draw_dim_reduction_plot(res, group = iris$Species,
                                 cluster_result = cl)
table(cluster = drawn$cluster, species = drawn$group)

## Names for the clusters the legend would otherwise number.
draw_dim_reduction_plot(res, cluster_result = cl,
                      cluster_lv = c("A", "B", "C"))

## A density method can place no point at all, and those are grey.
db <- cluster_dbscan(iris[1:4])
draw_dim_reduction_plot(res, cluster_result = db, asp = 1)

## The features as the points, labelled, on the third and fourth components.
by_feat <- perform_pca(iris[1:4], embedding_scale = "features")
draw_dim_reduction_plot(by_feat, dims = c(1, 3), anno_points = TRUE)

}
\seealso{
\code{\link[=perform_pca]{perform_pca()}} for the coordinates and \code{\link[=cluster_kmeans]{cluster_kmeans()}} for the
labels, and \code{\link[=draw_heatmap]{draw_heatmap()}}, which shows the same wide input a cell at a
time rather than a point at a time.
}
