% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_factorial_groups.R
\name{compare_factorial_groups}
\alias{compare_factorial_groups}
\title{Analyse a crossed-factor design as one model}
\usage{
compare_factorial_groups(
  data,
  feats,
  factors,
  factor_lv = NULL,
  control_label = NULL,
  within = NULL,
  id = NULL,
  conf_level = 0.95,
  ss_type = c("III", "II", "I"),
  posthoc = TRUE,
  posthoc_alpha = 0.05,
  posthoc_scope = c("both", "marginal", "simple"),
  fc_mean = c("arith", "geom"),
  input_scale = c("raw", "log2"),
  p_adjust = "BH",
  diagnose = TRUE
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per observation
and one column per feature.}

\item{feats}{Character vector of numeric column names in \code{data} to test. One
row of \verb{$tests} and \verb{$effect} per entry, and one row of \verb{$terms} per entry
and term.}

\item{factors}{Named list of the crossed factors, each entry either the name of
a column of \code{data} or a vector with one entry per row of it. Its length is how
many factors are crossed, and there have to be at least two: one factor is
\code{\link[=compare_multiple_groups]{compare_multiple_groups()}}. The \strong{first factor is the primary one} and the
order of the list is the order the terms are listed in, which is also the
order Type I sums of squares are taken in.}

\item{factor_lv}{Named list giving the levels of each factor, with the
reference level first, or \code{NULL} to take the levels from the data in sorted
order. Naming it does two things sorting cannot: it fixes which level is the
reference that \code{effect} and the cell labels are read against, and it drops
the rows belonging to any level it leaves out. Every level it names has to be
present in \code{factors}. \code{control_label} fixes the reference on its own, for
when that is all there is to say.}

\item{control_label}{The level to hold as the reference, one name per factor
it points at, as a named list (\code{list(treatment = "control", sex = "male")})
or as a named character vector (\code{c(treatment = "control")}). The two shapes
say the same thing. The level it names moves to the front of its own factor
and the rest keep the order they were given, so the reference cell is the one
where every factor sits at the level named here and every fold change is
read against it. A factor it says nothing about is left as it arrived, which
is what makes pointing one factor of three a sentence rather than a rewrite
of all three. Defaults to \code{NULL}, the first level of every factor, which
under \code{factor_lv = NULL} is the first in sorted order.}

\item{within}{Names of the factors measured within subjects. \strong{Not implemented
in this version}: a non-empty value is an error rather than a design that is
silently analysed as though the repeated measurements were independent. The
argument exists so that the error names what is missing, which an
\verb{unused argument (within)} from \code{\link[=do.call]{do.call()}} would not.}

\item{id}{Subject identifier with one entry per row of \code{data}. Used only by a
within-subject design, so it is ignored with a warning here.}

\item{conf_level}{Confidence level for the post-hoc intervals.}

\item{ss_type}{Which sums of squares the term tests are built from, \code{"III"},
\code{"II"} or \code{"I"}. They agree on a balanced design and differ on an unbalanced
one; see "Unbalanced cells" below.}

\item{posthoc}{Logical. If \code{FALSE}, no pairwise stage runs and the result
carries no \verb{$posthoc} slot.}

\item{posthoc_alpha}{A contrast is computed when the term it belongs to has a
\code{pval_adj} at or below this value. Set it to 1 to compare every level of
every factor regardless of the term tests.}

\item{posthoc_scope}{Which contrasts the pairwise stage covers: \code{"marginal"}
for the comparisons a main effect is about, \code{"simple"} for the same
comparisons inside one combination of the other factors, or \code{"both"}.}

\item{fc_mean}{Which centre the fold change divides, \code{"arith"} for the
arithmetic mean or \code{"geom"} for the geometric mean. Defaults to \code{"geom"}
when \code{input_scale = "log2"} and to \code{"arith"} otherwise.}

\item{input_scale}{The scale \code{data} arrives on, \code{"raw"} or \code{"log2"}. On the
log2 scale each observation is raised back through \code{2^x} before the centres
are taken, so the ratios mean what they do for raw input. This changes the
\code{effect} table only, never the tests. See \code{\link[=compare_two_groups]{compare_two_groups()}} for the
full account.}

\item{p_adjust}{Multiplicity adjustment passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}, applied
along the feature axis. Use \code{"none"} to disable. In \verb{$terms} it is applied
within each term rather than over the whole table, for the reason given under
"Two axes" below.}

\item{diagnose}{Logical. If \code{TRUE}, the normality, homogeneity of variance and
outlier checks the model rests on are attached as \verb{$diagnostics}, taken over
the cells.}
}
\value{
A \code{sa_comparison} object with the layout \code{\link[=compare_multiple_groups]{compare_multiple_groups()}}
returns, minus \verb{$pairwise} and plus \verb{$terms} and \verb{$cells}. Its elements are

\describe{
\item{\code{analysis}}{\code{"factorial_comparison"}.}
\item{\code{features}}{The feature names, in the row order every table uses.}
\item{\code{design}}{\code{factor_lv}, \code{anova_type}, \code{n_factors}, \code{group_lv} (the
cell labels), \code{cell_n}, \code{n_empty_cells}, \code{paired}, \code{pairing}, \code{within},
\code{n_dropped} and \code{unmatched_ids}.}
\item{\code{parameters}}{The analysis choices as used, plus \code{n_posthoc}, the
number of features that entered the pairwise stage.}
\item{\code{effect}}{One row per feature: \code{n_used}, \code{n_cells}, \code{ref_center},
\code{extreme_cell}, \code{extreme_center}, \code{fold_change} and \code{log2fc}. The
reference cell is the one where every factor sits at its first level,
which \code{factor_lv} or \code{control_label} states, and
\code{extreme_cell} is whichever cell sits furthest from it on the log2 scale,
the same definition \code{simulate_factorial_groups()} records in
\code{truth$extreme_cell}.}
\item{\code{tests}}{One table, \code{anova_test}, holding the whole-model F test with
one row per feature.}
\item{\code{terms}}{One row per feature and model term, holding \code{features},
\code{terms}, \code{term_order}, \code{n_used}, \code{df}, \code{ss}, \code{ms}, \code{f_stat}, \code{df_error},
\code{eta_sq}, \code{partial_eta_sq}, \code{log2_effect}, \code{pval} and \code{pval_adj}.}
\item{\code{cells}}{One row per feature and cell of the grid, holding
\code{features}, one column per factor named after it and holding the level
name, \code{cell} (the dot-joined label, as in \code{design$group_lv}), \code{n},
\code{mean}, \code{sd} and \code{se}. \code{mean} is the arithmetic cell mean the model was
fitted on and \code{se} is \code{sqrt(ms_error / n)}, pooled over the whole model,
so the variance of a marginal mean over a set of cells is recoverable
from it. This is the table \code{\link[=draw_interaction_plot]{draw_interaction_plot()}} reads, and the one
part of the grid the rest of the result cannot be read backwards into.}
\item{\code{posthoc}}{\code{anova_test}, one row per feature and contrast, carrying
the post-hoc contract columns plus \code{factor} and \code{stratum}. Absent when
\code{posthoc = FALSE}.}
\item{\code{test_info}}{The method \code{id}, a readable \code{label} naming the ANOVA
that ran, and the post-hoc procedure that followed it.}
\item{\code{diagnostics}}{Assumption checks over the cells, or \code{NULL}.}
\item{\code{metadata}}{\code{package_version}, \code{r_version}, \code{platform} and an
ISO-8601 \code{timestamp}.}
}
}
\description{
The factorial counterpart of \code{\link[=compare_multiple_groups]{compare_multiple_groups()}}, and the one place in
the package where a scenario function fits \strong{one} model rather than running
every applicable test side by side. Two crossed factors are a two-way ANOVA,
three are a three-way ANOVA and more than three are a factorial ANOVA, and
those are three names for the same fully crossed linear model rather than three
procedures to choose between. Which name applies follows from
\code{length(factor_lv)}, so there is no argument for it; the answer is reported in
\code{design$anova_type} and in the readable label of the test.
}
\details{
What multiplies instead is the number of answers. One factor asks whether its
levels are alike; crossing a second one asks that of each factor and asks
whether either one's effect depends on the other, and those questions fail
separately. A single p-value cannot carry them, so the result has an axis the
sibling functions do not: \verb{$terms}, one row per feature and model term.

Features that cannot be tested do not abort the run. Their rows are filled with
\code{NA} and all such features are reported together in a single warning.
}
\section{Two axes}{

The whole-model test in \verb{$tests$anova_test} asks whether a feature responds to
the design at all. That is one question per feature, and it is exactly the
one-way ANOVA that treats the cells as groups: a fully crossed model is the
cell means model written in another basis, so the two fit the same values and
leave the same residuals. Keeping it there is what lets
\code{\link[=estimate_significance]{estimate_significance()}}, \code{\link[=print]{print()}}, \code{\link[=draw_forest_plot]{draw_forest_plot()}} and
\code{\link[=draw_volcano_plot]{draw_volcano_plot()}} read a factorial result without knowing that it is one.

Which \strong{part} of the design a feature responds to is the question a crossed
model was fitted to answer, and it has one answer per term. Those live in
\verb{$terms}, whose \code{terms} and \code{term_order} columns are the ones
\code{simulate_factorial_groups()} writes into \code{truth_term}, so a result and an
answer key merge on \code{c("features", "terms")} with neither side renamed.

\code{pval_adj} is adjusted within each term, across features. A term is one family:
asking of five hundred features whether the treatment matters is five hundred
instances of one question, while asking whether the treatment matters and
whether it depends on sex are two questions and pooling them would correct each
for the other's multiplicity.

There is no \verb{$pairwise} slot. Its keys are contrast labels, and a factorial
design has a two-dimensional contrast axis, factor by stratum, that a flat
list keyed by label cannot hold without a naming convention. Until there is
one, \code{estimate_significance(by = "contrast")} reports that the result has no
pairwise stage, which is the true statement.
}

\section{The size of a term}{

\code{eta_sq} and \code{partial_eta_sq} say how much of the variance a term accounts for,
and neither has a sign, so neither can be read as a direction. \code{log2_effect}
can: it is the largest ANOVA component of the term, with its sign, taken by
decomposing \code{log2()} of the same cell centres \code{effect} is built from. It is
what \code{\link[=estimate_significance]{estimate_significance()}} puts on the effect axis in a term reading, and
so what \code{\link[=draw_volcano_plot]{draw_volcano_plot()}} plots a term panel against when passed the
verdict of \code{estimate_significance(fact, by = "term")}.

A component is a deviation from what the rest of the model already predicts,
\strong{not} a difference between two levels. A two-level factor whose levels differ
by one log2 unit has components \code{-0.5} and \code{+0.5}, so \code{log2_effect} is \code{0.5}
where the marginal fold change is 2. The definition is kept because it is the
one \code{simulate_factorial_groups()} records in \code{truth_term$max_abs_delta}, which
makes the column scorable against an answer key; the consequence is that
cutoffs meant for a fold change, \code{log2fc_cutoff = 1} among them, are stricter
here than they look.

Components do not depend on which cell is the reference, since subtracting one
cell from every cell cancels out of the deviation. That is the one place the
term axis is simpler than \code{effect$log2fc}, which is read against the reference
cell by definition.
}

\section{Unbalanced cells}{

With equal cell sizes the three types of sums of squares are identical and
\code{ss_type} changes nothing. They part company when the cells are unequal,
because the factors are then no longer orthogonal and some of the variation
can be attributed to more than one of them.

The default is \strong{Type III}, each term adjusted for every other term. It is the
type whose main effects are statements about the levels rather than about how
many observations happened to land in each, which is what makes it agree with
the unweighted marginal means the post-hoc stage compares and with the
decomposition \code{simulate_factorial_groups()} plants. \strong{Type II} adjusts a term
for every term that does not contain it, leaving a main effect unadjusted for
the interaction it is part of. \strong{Type I} is sequential and therefore depends
on the order \code{factors} was written in; it is what \code{\link[stats:aov]{stats::aov()}} reports, which
makes it the type to ask for when checking these numbers against an external
implementation on unbalanced data.

A cell holding no usable observation leaves the crossed model with nothing to
estimate there, so that feature's rows are \code{NA} and the reason is reported in
one warning. \code{design$n_empty_cells} counts the cells that hold no rows at all.
}

\section{Marginal contrasts and simple effects}{

A marginal contrast compares two levels of one factor with the other factors
averaged away, which is the comparison a main effect is a statement about. A
simple effect compares the same two levels inside one combination of the other
factors, and it is the only one of the two that means anything when an
interaction is real: if the treatment helps males and harms females, the
average of the two is a number that describes nobody. \code{stratum} tells them
apart, \code{NA} for a marginal contrast and the levels of the other factors joined
by a dot for a simple one, and \code{factor} names the factor being compared. Both
columns are the ones \code{truth_contrast} carries.

The marginal mean is the \strong{unweighted} mean of the cell means rather than the
mean of the observations, so a level is not pulled towards whichever
combination of the other factors was sampled most heavily. Every contrast is
scaled by the mean square error of the whole model, which is what makes the
pairwise stage consistent with the term tests instead of a second analysis of
the same data, and judged against the studentised range over the number of
levels of its own factor. Those p-values are already family-wise within a
block, so there is no \code{posthoc_p_adjust} argument to apply and
\code{parameters$posthoc_p_adjust} is \code{NA}.

Which contrasts are computed is decided term by term. A marginal contrast of a
factor runs when that factor's main effect cleared \code{posthoc_alpha}, and a
simple effect runs when the interaction of the factor with the factors held
fixed cleared it. Gating everything on the whole-model test instead would
compare the levels of a factor that the model says has no effect, on the
strength of a different factor that has one.
}

\examples{
## Two crossed factors, so a two-way ANOVA. The factors name columns of `data`.
res <- compare_factorial_groups(
  data    = warpbreaks,
  feats   = "breaks",
  factors = list(wool = "wool", tension = "tension")
)
res

## The whole-model test, one row per feature: does this feature respond at all.
res$tests$anova_test

## The answer per term, which is what the design was crossed to get.
res$terms

## Every fold change is read against the cell where each factor sits at its
## first level. `control_label` moves one of those levels without the rest
## having to be listed, so the reference cell becomes A.M rather than A.L.
medium <- compare_factorial_groups(
  data          = warpbreaks,
  feats         = "breaks",
  factors       = list(wool = "wool", tension = "tension"),
  control_label = list(tension = "M"),
  posthoc       = FALSE
)
medium$design$group_lv[1]
medium$effect[c("ref_center", "extreme_cell", "log2fc")]

## The whole-model volcano names the reference cell on the x axis.
draw_volcano_plot(estimate_significance(res, log2fc_cutoff = 0.1))

## Marginal contrasts and simple effects, told apart by `stratum`.
subset(res$posthoc$anova_test, factor == "tension" & is.na(stratum))

## A simulated design hands over exactly the arguments this function takes,
## and `truth_term` is the answer key for `$terms`.
sim <- simulate_factorial_groups(n_feats = 12, n_per_cell = 8, seed = 1)
fac <- do.call(compare_factorial_groups, sim$args)
scored <- merge(fac$terms, sim$truth_term, by = c("features", "terms"))
with(scored, table(called = pval_adj <= 0.05, planted = is_effect, terms))

}
\references{
Fisher, R. A. (1935). \emph{The Design of Experiments}.

Yates, F. (1934). The analysis of multiple classifications with unequal numbers
in the different classes. \emph{Journal of the American Statistical Association},
29(185), 51-66.

Tukey, J. W. (1949). Comparing individual means in the analysis of variance.
\emph{Biometrics}, 5(2), 99-114.

Kramer, C. Y. (1956). Extension of multiple range tests to group means with
unequal numbers of replications. \emph{Biometrics}, 12(3), 307-310.
}
\seealso{
\code{\link[=compare_multiple_groups]{compare_multiple_groups()}} for a single factor,
\code{\link[=simulate_factorial_groups]{simulate_factorial_groups()}} for data whose answer per term is known, and
\code{\link[=draw_forest_plot]{draw_forest_plot()}} to draw the result.
}
