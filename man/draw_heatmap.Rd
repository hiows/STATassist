% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_heatmap.R
\name{draw_heatmap}
\alias{draw_heatmap}
\title{Draw a clustered heatmap of features by samples}
\usage{
draw_heatmap(
  data,
  group = NULL,
  group_lv = NULL,
  feats = NULL,
  scale = c("feature", "sample", "none"),
  zlim = NULL,
  dist_method = c("euclidean", "correlation", "manhattan"),
  hclust_method = c("average", "complete", "ward.D2"),
  cluster_feats = TRUE,
  cluster_samples = TRUE,
  feat_labels = NULL,
  sample_labels = NULL,
  show_feat_names = TRUE,
  show_sample_names = TRUE,
  anno = FALSE,
  cex.anno = 1,
  n_colors = 101,
  main = NULL,
  cex.axis = 0.9,
  cex.main = 1.5,
  cex.legend = 1.2
)
}
\arguments{
\item{data}{A data.frame or a matrix in wide format, one row per observation
and one column per feature. This is the same layout
\code{\link[=compare_two_groups]{compare_two_groups()}} and \code{\link[=compare_multiple_groups]{compare_multiple_groups()}} take. Either type is
accepted and converted to the numeric matrix the drawing needs; a matrix
with no column names has them made up as \code{V1}, \code{V2} and so on, and repeated
row names are kept, since a sample name is a naming choice rather than a
key. There must be at least two features and two samples to draw.}

\item{group}{Grouping vector with one entry per row of \code{data}, or \code{NULL} when
\code{group_lv} is also \code{NULL} to draw without a group strip or group legend.}

\item{group_lv}{Character vector of group levels, in the order they should
appear in the legend, or \code{NULL} with \code{group = NULL}. Rows belonging to any
other level are dropped when both are supplied.}

\item{feats}{Character vector of numeric column names to draw, or \code{NULL} for
every column of \code{data}. The order given is the order the rows are drawn in
when they are not clustered.}

\item{scale}{How to put the features on a comparable scale before drawing:
\code{"feature"} z-scores each feature across the samples, \code{"sample"} z-scores
each sample across the features, and \code{"none"} draws the values as they
arrived. The scaled values are what \code{matrix} comes back holding.}

\item{zlim}{Numeric length-2 range the colours span, or \code{NULL} to derive it
from the values being drawn. Values outside a supplied range are drawn at
the end of the scale rather than left blank, and how many were is reported.}

\item{dist_method}{Distance behind the clustering. \code{"correlation"} is
\code{1 - cor()}, which groups features by the shape of their profile rather
than by how high it sits; the other two are handed to \code{\link[stats:dist]{stats::dist()}}.}

\item{hclust_method}{Linkage handed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{cluster_feats, cluster_samples}{Whether to cluster and reorder that
axis. \code{FALSE} keeps the input order.}

\item{feat_labels, sample_labels}{Labels to draw in place of the column names
of \code{data} and its row names. \code{sample_labels} has one entry per row of
\code{data} and is filtered along with it; \code{feat_labels} has one entry per
feature in \code{feats}.}

\item{show_feat_names, show_sample_names}{Whether to draw those labels. The
labels still exist when they are not drawn, so a hidden axis does not
change anything else about the plot.}

\item{anno}{If \code{TRUE}, write each cell value on the cell, rounded to two
decimal places. The numbers are the values in the returned \code{matrix} (after
\code{scale}, and not clamped to \code{zlim}). Missing cells are left blank.}

\item{cex.anno}{Character expansion for those cell labels, relative to the
smaller of the feature and sample axis label sizes.}

\item{n_colors}{Number of colours in the blue-white-red ramp.}

\item{main}{Plot title.}

\item{cex.axis, cex.main, cex.legend}{Character expansion for the axis labels,
the title and the group legend.}
}
\value{
A list, invisibly:

\describe{
\item{\code{matrix}}{The scaled matrix as it was drawn: features in rows,
samples in columns, both in the order they appear on the plot, and the
row and column names as they were labelled. Values are not clamped to
\code{zlim}, which is a decision about colour rather than about the data.}
\item{\code{feat_order},\code{sample_order}}{The permutations the clustering chose,
as indices into \code{feats} and into the rows of \code{data} that were kept.}
\item{\code{feat_hclust},\code{sample_hclust}}{The \code{\link[stats:hclust]{stats::hclust()}} objects behind
the dendrograms, or \code{NULL} for an axis that was not clustered.}
\item{\code{zlim}}{The colour range, derived or as supplied.}
\item{\code{group_colors}}{The colour drawn for each group level, or \code{NULL}
when no group was supplied.}
}
}
\description{
Draws one cell per feature and sample, with the sample groups as a coloured
annotation strip above the columns and a dendrogram on each axis that was
clustered. The input is the wide format the comparison functions take, one row
per observation and one column per feature, and it is transposed so that
features run down the rows the way an expression heatmap is usually read.
\code{\link[stats:heatmap]{stats::heatmap()}} draws the cells, the strip and the trees; this function
decides what goes into them and adds the colour key beside them.
}
\details{
Features are z-scored across the samples by default. Without it a single
high-abundance feature takes the whole colour range and everything else is
left white, since the colour scale is shared by every cell in the plot and
features are not measured on a common scale. The plot does not say which
\code{scale} ran: naming it would take a line of text wider than the key it titles,
and the numbers beside the key are the scaled values either way. A
feature with no variance would divide by zero, so it is only centred and ends
up flat at the middle of the scale; how many were is reported in a message.

A diverging palette needs a meaningful midpoint. When \code{zlim} is not given,
zero is that midpoint if the values being drawn have both signs, which is
always the case after z-scoring, and the range is made symmetric around it.
Values of one sign only have no such point, so the range is the range of the
values and white falls in the middle of it.

Missing values are drawn as grey cells rather than dropped, so a gap is
visible as a gap. Clustering leaves them to \code{\link[stats:dist]{stats::dist()}}, which measures a
pair on the values they share. A pair that shares none has no distance at
all, and rather than fail, that axis is left in its input order with a
message saying so.

The clustering is done here rather than left to \code{\link[stats:heatmap]{stats::heatmap()}}, which
would standardise after clustering and divide a flat feature by zero, and the
trees are handed over already built. What comes back is therefore the tree the
plot shows. The panel proportions are \code{\link[stats:heatmap]{stats::heatmap()}}'s, including the
square cells its \code{respect = TRUE} layout asks for. That layout keeps the block
of panels square and centres it, so a device much wider than it is tall is left
with empty space to the right of the plot and its key.

\code{\link[stats:heatmap]{stats::heatmap()}} draws no colour key, so this function keeps a strip beside
the plot for one and overlays it after: an upright bar with its numbers to the
right of it, and the group legend to the right of those, both starting level
with the top row of cells. The strip is as wide as those need rather than a
fixed fraction of the device, and it is placed against the feature labels
rather than at the edge, since the space the centred layout leaves over would
otherwise read as a gap between the plot and its key. The panel layout, the
margins and the plot region are all restored on exit, so the caller's device
is left as it was found.
}
\examples{
## Features are z-scored, so the two planted directions separate by colour
## and the clustering of the columns finds the two groups on its own.
sim <- simulate_two_groups(n_feats = 30, n_up = 5, n_down = 5, seed = 3)
out <- draw_heatmap(
  data     = as.matrix(sim$args$data),
  group    = sim$args$group,
  group_lv = sim$args$group_lv,
  show_sample_names = FALSE,
  main     = "30 features, 10 of them planted"
)

## The clustering is on the result, so what the picture shows can be checked
## rather than eyeballed.
head(rownames(out$matrix))
out$sample_hclust$method

## Only the planted features, in the order they were named
draw_heatmap(
  data          = sim$args$data,
  group         = sim$args$group,
  group_lv      = sim$args$group_lv,
  feats         = sim$truth$features[sim$truth$direction != "none"],
  cluster_feats = FALSE,
  show_sample_names = FALSE
)

## Unscaled values, with the four measurements on their own scale
draw_heatmap(
  data     = iris[1:4],
  group    = iris$Species,
  group_lv = levels(iris$Species),
  scale    = "none",
  show_sample_names = FALSE,
  main     = "iris, cm"
)

## No group strip or group legend when both are omitted
draw_heatmap(
  data = as.matrix(sim$args$data),
  show_sample_names = FALSE
)

}
\seealso{
\code{\link[=draw_grouped_boxplot]{draw_grouped_boxplot()}}, which takes the same wide input.
}
