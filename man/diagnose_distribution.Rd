% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_distribution.R
\name{diagnose_distribution}
\alias{diagnose_distribution}
\title{Check the assumptions a comparison rests on}
\usage{
diagnose_distribution(
  data,
  feats,
  group = NULL,
  group_lv = NULL,
  alpha = 0.05,
  criterion = c("iqr", "robust_z", "grubbs"),
  iqr_multiplier = 1.5,
  z_threshold = 3.5,
  center = c("median", "mean", "trimmed"),
  trim = 0.1
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per
observation and one column per feature.}

\item{feats}{Character vector of numeric column names in \code{data} to check.}

\item{group}{Optional grouping vector with one entry per row of \code{data}. When
supplied, normality is checked within each level and the variance tests are
run across them. Without it there is only one sample per feature, so the
variance table is empty.}

\item{group_lv}{Group levels to keep, in display order. Defaults to the
sorted unique values of \code{group}.}

\item{alpha}{Threshold applied to the p-values when setting the \code{normal_ok}
and \code{variance_ok} flags of the summary table.}

\item{criterion, iqr_multiplier, z_threshold}{Passed to \code{\link[=screen_outliers]{screen_outliers()}}.}

\item{center, trim}{Centre used by the Levene test and its trimming
proportion, passed through to the Brown-Forsythe variant.}
}
\value{
A \code{sa_diagnosis} object: a plain list carrying \code{analysis},
\code{features}, \code{design}, \code{parameters}, \code{metadata} and four tables.

\describe{
\item{\code{normality}}{One row per feature and group level: \code{features},
\code{group}, \code{n_used}, \code{shapiro_stat}, \code{shapiro_pval}, \code{ks_stat},
\code{ks_pval}, \code{skewness} and \code{excess_kurtosis}. The level column is named
\code{group} to match \code{\link[=summarize_descriptive_stats]{summarize_descriptive_stats()}}, and is \code{NA} when no
grouping was supplied.}
\item{\code{variance}}{One row per feature: \code{features}, \code{n_used}, \code{n_groups},
\code{levene_stat}, \code{levene_df1}, \code{levene_df2}, \code{levene_pval},
\code{bartlett_stat}, \code{bartlett_df} and \code{bartlett_pval}. Zero rows when no
\code{group} was supplied.}
\item{\code{outliers}}{The \code{\link[=screen_outliers]{screen_outliers()}} table, one row per flagged
observation.}
\item{\code{summary}}{One row per feature: \code{features}, \code{n_levels},
\code{n_outliers}, \code{min_shapiro_pval}, \code{normal_ok} and \code{variance_ok}. The
two flags are \code{NA} when the corresponding test could not be run.}
}

The result is deliberately not an \code{sa_comparison}. Normality is a property
of one sample and homogeneity a property of a set of them, so the two
tables have different numbers of rows and would not fit a contract built
around one row per feature.
}
\description{
Runs the normality tests, the homogeneity of variance tests and the outlier
screen together, and reports them as one object. The same checks are attached
automatically to \code{\link[=compare_two_groups]{compare_two_groups()}} and \code{\link[=compare_multiple_groups]{compare_multiple_groups()}}, so
an assumption is never silently ignored; this function is for looking at them
on their own, before a test has been chosen.
}
\details{
A failed assumption never blocks an analysis and never causes a test to be
swapped for another one. It changes which member of the reported test family
deserves the most weight, and that judgement stays with the user. Skewed
groups make the rank-based and robust members more trustworthy than the
parametric one; unequal variances make Welch's and Brunner-Munzel's
treatments of the same data more trustworthy than the pooled ones.

Each assumption is checked twice on purpose, by tests that fail differently:

\describe{
\item{Normality}{Shapiro-Wilk is the more powerful of the two and is the
one to read first. The Kolmogorov-Smirnov test is fitted against a normal
with the sample's own mean and standard deviation, which makes its
p-value anti-conservative, so it disagreeing with Shapiro-Wilk usually
means the departure is in the tails.}
\item{Homogeneity of variance}{The Levene test is centred on the median,
the Brown-Forsythe variant, and tolerates skew. Bartlett's test is more
powerful when the groups really are normal and cannot tell unequal
variances apart from heavy tails when they are not. The two disagreeing
is itself evidence about normality.}
}
}
\examples{
feats <- c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width")

## Within species, so a genuine species difference is not read as skew
d <- diagnose_distribution(iris, feats, iris$Species)
d
d$normality
d$variance
d$summary

## Petal length pooled over species is strongly bimodal, which every
## normality test picks up once the grouping is taken away.
diagnose_distribution(iris, "Petal.Length")$normality

}
\references{
Shapiro, S. S. and Wilk, M. B. (1965). An analysis of variance test for
normality (complete samples). \emph{Biometrika}, 52(3-4), 591-611.

Massey, F. J. (1951). The Kolmogorov-Smirnov test for goodness of fit.
\emph{Journal of the American Statistical Association}, 46(253), 68-78.

Brown, M. B. and Forsythe, A. B. (1974). Robust tests for the equality of
variances. \emph{Journal of the American Statistical Association}, 69(346),
364-367.

Bartlett, M. S. (1937). Properties of sufficiency and statistical tests.
\emph{Proceedings of the Royal Society A}, 160(901), 268-282.
}
\seealso{
\code{\link[=screen_outliers]{screen_outliers()}} for the outlier stage on its own, and
\code{\link[=compare_multiple_groups]{compare_multiple_groups()}}, whose \verb{$diagnostics} slot holds the same
tables for the data it tested.
}
