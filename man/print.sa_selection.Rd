% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{print.sa_selection}
\alias{print.sa_selection}
\title{Print a feature selection}
\usage{
\method{print}{sa_selection}(x, n = 10L, ...)
}
\arguments{
\item{x}{A selection, as returned by \code{\link[=perform_rfe]{perform_rfe()}} or \code{\link[=perform_stepwise]{perform_stepwise()}}.}

\item{n}{Maximum number of candidates to show. The rest are counted.}

\item{...}{Ignored, present for consistency with \code{\link[=print]{print()}}.}
}
\value{
\code{x} invisibly.
}
\description{
Summarises what was searched and what was kept, rather than printing every
table. The score of every model the search compared is in \code{x$profile}, which is
what says whether the answer won by much or by nothing, and the engine object is
\code{x$fit}.
}
\examples{
perform_rfe(mtcars, outcome = "mpg", predictors = c("wt", "hp", "disp"),
            cv_method = "kfold", n_fold = 3, seed = 1)

## The same method, on a search that walked a path rather than a ladder of
## subset sizes and that held no rows out to score it.
perform_stepwise(mtcars, outcome = "mpg", predictors = c("wt", "hp", "disp"))

}
