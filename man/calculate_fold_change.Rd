% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_fold_change.R
\name{calculate_fold_change}
\alias{calculate_fold_change}
\title{Fold change between two groups, feature by feature}
\usage{
calculate_fold_change(
  data,
  feats,
  group,
  group_lv,
  case_label = group_lv[1],
  mean_type = c("arith", "geom")
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per
observation and one column per feature.}

\item{feats}{Character vector of numeric column names in \code{data}. One
returned value per entry, in this order.}

\item{group}{Grouping vector with one entry per row of \code{data}.}

\item{group_lv}{Character vector of exactly two group levels. Rows belonging
to any other level are dropped.}

\item{case_label}{The level of \code{group_lv} that goes in the numerator. The
remaining level becomes the denominator, so a value above 1 means higher in
\code{case_label}.}

\item{mean_type}{Which mean to divide, \code{"arith"} for the arithmetic mean or
\code{"geom"} for the geometric mean. The geometric mean requires strictly
positive values.}
}
\value{
Numeric vector of fold changes named by \code{feats}.
}
\description{
Divides the case group mean by the control group mean for every feature, the
quantity a volcano plot puts on its x axis once logged.
}
\details{
Missing values are dropped within each group, so the two groups may rest on
different numbers of observations for different features.

A feature that cannot be summarised does not abort the run. Its entry is
\code{NA} and all such features are reported together in a single warning. This
covers a group left empty after dropping \code{NA} and, under
\code{mean_type = "geom"}, any value at or below zero: \code{log()} turns those into
\code{NaN} or \code{-Inf}, and dropping them would quietly return the geometric mean of
the positive subset instead.

A fold change is a ratio, so it only reads as "n times higher" when both
means are positive. Features whose control mean is zero (giving \code{Inf}) or
whose means differ in sign are therefore reported in a \code{message()}.
}
\examples{
iris2 <- iris[iris$Species != "setosa", ]
feats <- c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width")

## virginica relative to versicolor
calculate_fold_change(
  data       = iris2,
  feats      = feats,
  group      = iris2$Species,
  group_lv   = c("versicolor", "virginica"),
  case_label = "virginica"
)

## The default numerator is the first level, which inverts every ratio.
calculate_fold_change(iris2, feats, iris2$Species,
                      c("versicolor", "virginica"))

## Geometric means are the usual choice for concentration-like data.
calculate_fold_change(iris2, feats, iris2$Species,
                      c("versicolor", "virginica"),
                      mean_type = "geom")

}
\seealso{
\code{\link[=evaluate_significance]{evaluate_significance()}} to combine the result with p-values, and
\code{\link[=compare_two_groups]{compare_two_groups()}} to obtain those p-values.
}
