% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{predict.sa_model}
\alias{predict.sa_model}
\title{Predict from a fitted model on rows it was not fitted to}
\usage{
\method{predict}{sa_model}(object, newdata = NULL, type = c("raw", "response", "prob"), ...)
}
\arguments{
\item{object}{A fitted model, as returned by \code{\link[=fit_linear_regression]{fit_linear_regression()}},
\code{\link[=fit_logistic_regression]{fit_logistic_regression()}}, \code{\link[=fit_elastic_net]{fit_elastic_net()}}, \code{\link[=fit_rf]{fit_rf()}} or
\code{\link[=fit_svm]{fit_svm()}}.}

\item{newdata}{Rows to predict, a data.frame or matrix carrying the predictor
columns, or \code{NULL} for the rows the model was fitted on.}

\item{type}{\code{"raw"} for the prediction itself, a fitted value for a regression
and a class label for a classification; \code{"response"} for the prediction on
the scale of the outcome, which for a classification is the probability of
\code{design$outcome_lv[2]}, the class the coefficients describe; or \code{"prob"} for
one column per class.}

\item{...}{Passed on to \code{\link[caret:predict.train]{caret::predict.train()}}.}
}
\value{
One prediction per row of \code{newdata}: an unnamed numeric vector for a
regression and for \code{type = "response"}, a factor at the levels of
\code{design$outcome_lv} for \code{type = "raw"} on a classification, and a data.frame
of one column per class for \code{type = "prob"}. Rows that are not complete
across the predictors are \code{NA} throughout.
}
\description{
Takes the data frame the fit took — the test half of a \code{\link[=split_data]{split_data()}} result,
say — and answers one prediction per row of it. The columns are read by name
and coded the way the fit coded them, so the rows to predict can be handed over
exactly as they came, outcome column and all.
}
\details{
This is the method to use rather than \code{predict(object$fit, newdata = )}. The
engine object knows the names of the columns it was given and nothing about
where they came from, which is enough for \code{\link[=fit_linear_regression]{fit_linear_regression()}},
\code{\link[=fit_logistic_regression]{fit_logistic_regression()}} and \code{\link[=fit_rf]{fit_rf()}}, whose engine was handed the
predictor frame itself, and not enough for \code{\link[=fit_elastic_net]{fit_elastic_net()}} or \code{\link[=fit_svm]{fit_svm()}},
whose engines were handed a design matrix. \code{glmnet} and \code{kernlab} read that
matrix by position, and \code{caret} prepares \code{newdata} for it by keeping the columns
whose names it recognises, in the order \code{newdata} happens to hold them. A factor
predictor is therefore dropped whole, since \code{x_cat} is not one of the names —
the model has \code{x_catmid} and \code{x_cathigh} — and a set of numeric predictors in
another order is silently matched to the wrong coefficients, or measured along
the wrong axis of the kernel. Here the terms are rebuilt from the levels the fit
recorded and put in the model's own order by name, which is what makes one call
work for every model in the family.

Columns the model never saw are ignored, so the outcome column and anything
else the frame carries can stay. A predictor that is absent is an error naming
it, and so is a factor level the fit never saw, since neither has a coefficient
to be predicted with.

There is one prediction per row of \code{newdata} whatever the row holds, and a row
with a missing value among the predictors gets \code{NA}. That is the rule the fit
already follows in reverse: those are the rows \code{design$n_dropped} counted, and
they cannot be predicted for the same reason they could not be fitted. Saying
so with an \code{NA} in place keeps the answer aligned with \code{newdata}, which a
shorter vector would not be. It also has to be said explicitly, because a
penalized fit would otherwise predict some of them: a coefficient of exactly
zero drops out of the sparse product before the missing value it multiplies is
ever read, so whether a row could be predicted would depend on which predictor
the hole fell in.
}
\examples{
sp <- split_data(mtcars, seed = 1)
train <- sp$datasets[[1]]$train_data
test <- sp$datasets[[1]]$test_data

fit <- fit_linear_regression(train, outcome = "mpg",
                             predictors = c("wt", "hp"), cv = FALSE)
sqrt(mean((test$mpg - predict(fit, newdata = test))^2))

## A factor predictor is what `predict(fit$fit, )` cannot be given on a
## penalized fit: the model has one term per level, and the frame has the
## column the levels came from.
train$cyl <- factor(train$cyl)
test$cyl <- factor(test$cyl, levels = levels(train$cyl))
pen <- fit_elastic_net(train, outcome = "mpg",
                       predictors = c("wt", "hp", "cyl"),
                       penalty = "lasso", lambda = 0.5, cv = FALSE)
predict(pen, newdata = test)

## On a classification, `type = "response"` is the probability of the second
## level of `outcome_lv`, the one the coefficients describe.
iris2 <- iris[iris$Species != "setosa", ]
clf <- fit_logistic_regression(iris2, outcome = "Species",
                               predictors = "Petal.Length",
                               outcome_lv = c("versicolor", "virginica"),
                               cv = FALSE)
head(predict(clf, newdata = iris2, type = "response"))

}
\seealso{
\code{\link[=fit_elastic_net]{fit_elastic_net()}}, \code{\link[=fit_rf]{fit_rf()}}, \code{\link[=fit_svm]{fit_svm()}}, \code{\link[=split_data]{split_data()}}, which
draws the rows to predict, and \code{\link[=predict.sa_fit]{predict.sa_fit()}} for the engine object in
\verb{$fit}, which is \code{caret}'s own method and takes what \code{caret} prepared.
}
