% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_volcano_plot.R
\name{draw_volcano_plot}
\alias{draw_volcano_plot}
\title{Draw a volcano plot}
\usage{
draw_volcano_plot(
  significance_result,
  terms = NULL,
  panel_nrow = NULL,
  use_adjusted = TRUE,
  log2fc_cutoff = NULL,
  pval_cutoff = NULL,
  anno_feats = TRUE,
  anno_top = 10,
  cex.anno = 1,
  xlim = NULL,
  ylim = NULL,
  xlab = NULL,
  main = NULL,
  cex.lab = 1.3,
  cex.axis = 1.2,
  cex.main = 1.3,
  margin = c(5, 5, 4, 3),
  ...
)
}
\arguments{
\item{significance_result}{The object returned by
\code{\link[=estimate_significance]{estimate_significance()}}, whose \code{significance} element is what is plotted.
With \code{by = "contrast"} that element holds one table per contrast, so name
the one to draw: \code{sig$significance[["virginica - setosa"]]}. With
\code{by = "term"} the whole list is drawn, one panel per term. A bare verdict
data.frame is accepted too.}

\item{terms}{Which terms get a panel, read only under a term reading. \code{NULL},
the default, draws the two main effects of the first two factors and their
interaction, which is every term of a two-factor design and three of the
seven of a three-factor one; the terms left out are named in a \code{message()}.
Give term labels, as \verb{$terms$terms} spells them, to draw those instead.}

\item{panel_nrow}{How many rows the panels are laid out in, or \code{NULL} for a
single row, which is what three panels of a two-factor design want.}

\item{use_adjusted}{Logical. If \code{TRUE}, plot and threshold the \code{adj_pvalue}
column; if \code{FALSE}, use the unadjusted \code{pvalue} column. The axis label
follows, so the y axis always describes what was actually plotted.}

\item{log2fc_cutoff, pval_cutoff}{Cutoffs for calling a feature changed and
significant, drawn as guides. \code{NULL}, the default, takes the values
\code{\link[=estimate_significance]{estimate_significance()}} recorded on \code{significance_result}, so the guides
agree with its \code{is_signif} column. Supply a number to override them.
Selecting a subset of rows keeps the recorded values, but selecting
columns drops them, in which case both must be given. Under a term reading
the magnitude cutoff is applied to \code{abs(log2_effect)}.}

\item{anno_feats}{Logical. If \code{TRUE}, label the strongest significant
features. A run where no feature clears both cutoffs still draws the plot,
with a \code{message()} in place of the labels.}

\item{anno_top}{How many features to label in each direction under a signed
reading (up to \code{2 * anno_top} in total), or how many significant features
to label under a term reading.}

\item{cex.anno}{Character expansion for those labels.}

\item{xlim, ylim}{Numeric length-2 axis ranges, or \code{NULL} to derive them from
the data. A supplied range is used as given. Across panels a derived range
is derived from all of them at once, so the panels can be compared. Term
panels start at 0 on the x axis unless \code{xlim} says otherwise.}

\item{xlab}{X axis label, or \code{NULL} to derive it from the effect column the
verdict carries.}

\item{main}{Plot title. With panels it is the title of the figure, written
once above them, and each panel is titled with its term.}

\item{cex.lab, cex.axis, cex.main}{Character expansion for the axis labels,
the axis annotation and the title.}

\item{margin}{Plot margins in lines, passed to \code{\link[graphics:par]{graphics::par()}} as \code{mar}.
Applied to every panel.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
\code{NULL} invisibly.
}
\description{
Plots the effect size against \code{-log10(pvalue)} for the table
\code{\link[=estimate_significance]{estimate_significance()}} returns, colours the features that clear both
cutoffs and labels the strongest of them. Under an omnibus or contrast
reading the effect is signed \code{log2fc} and points are coloured by direction.
A term reading of a factorial comparison is drawn as one panel per term, with
\verb{|log2_effect|} on the x axis and a single significance colour.
}
\details{
The plot margins are restored on exit, but the coordinate system is
deliberately left in place, so \code{\link[graphics:points]{graphics::points()}}, \code{\link[graphics:text]{graphics::text()}} and
friends can still be used to add to the finished plot. With panels what is
left in place is the last panel's coordinate system, and the panel grid itself
is undone, \code{mfrow} included, since a figure of several plots is finished when
the last of them is drawn.

A p-value of exactly zero has no finite \code{-log10()}. Rather than dropping the
most significant points or letting an infinite axis limit blank the plot, the
axis is scaled to the largest finite value and those points are drawn at the
top of it. Non-finite \code{log2fc}, which \code{\link[=compare_two_groups]{compare_two_groups()}} produces when a
group centre is zero, is capped at the edge of the x axis the same way. Either
kind of capping is reported in a \code{message()}, since a capped point no longer
sits at its true coordinate.

What the x axis compares is not the same question in every scenario, so the
label follows the comparison the verdict came from. A multi-group omnibus
verdict carries the single fold change its \code{effect} table holds, the level
furthest from the reference rather than any named pair, and the label says so.
A factorial omnibus verdict does the same with cells: the reference cell is
where every factor sits at its first level, and the label names it. A term
table carries an ANOVA component rather than a ratio of two centres, so the
panel plots \verb{|log2_effect|} and is labelled as an absolute effect rather than
a fold change; see "The size of a term" in \code{\link[=compare_factorial_groups]{compare_factorial_groups()}} for
how to read its magnitude. Every other reading compares two fixed centres and
is labelled \verb{log2 FC}. Pass \code{xlab} to override this.

Points are coloured by the same masks that select the labels, so which
features are highlighted and which are labelled can never disagree. With the
default arguments those masks reproduce the \code{is_signif} column of the input.
Signed readings colour up and down separately; a term panel draws significant
points in black because the axis has no direction, and keeps labels red so
they stay legible without being read as up-regulation. The labels are drawn
in a brighter shade than the points on purpose, so that a label stays
legible where it overlaps them.
}
\section{One panel per term}{

A term reading is drawn as a figure rather than asked to name one table,
unlike a contrast reading. A crossed design decomposes into a fixed and small
set of terms, three for two factors, and the whole point of the reading is to
see which of them a feature responded to, which is a comparison between the
panels. The number of pairwise contrasts, by contrast, follows from the level
counts and is arbitrary.

Every panel is judged by one rule, the cutoffs of the first table, and shares
the axes with the others unless \code{xlim} or \code{ylim} says otherwise. Both are what
makes a point in one panel comparable with a point in another. The shared x
axis starts at zero and the magnitude cutoff is drawn as a single vertical
guide, because the plotted quantity is \verb{|log2_effect|} rather than a signed
fold change. Significant points are black; labels stay red so they are not
read as up-regulation.
}

\examples{
iris2 <- iris[iris$Species != "setosa", ]
res <- compare_two_groups(
  data     = iris2,
  feats    = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width"),
  group    = iris2$Species,
  group_lv = c("virginica", "versicolor")
)
sig <- estimate_significance(res, log2fc_cutoff = 0.1)

draw_volcano_plot(sig, main = "virginica vs versicolor")

## Unadjusted p-values, no labels
draw_volcano_plot(sig, use_adjusted = FALSE, anno_feats = FALSE)

## A cutoff other than the one the verdict used
draw_volcano_plot(sig, log2fc_cutoff = 0.3)

## A multi-group verdict says on the x axis which levels its log2fc compares
multi <- compare_multiple_groups(iris, c("Sepal.Length", "Petal.Length"),
                                 iris$Species, levels(iris$Species))
draw_volcano_plot(estimate_significance(multi, log2fc_cutoff = 0.1))

## A factorial omnibus verdict names the reference cell on the x axis
fact <- compare_factorial_groups(
  data    = warpbreaks,
  feats   = "breaks",
  factors = list(wool = "wool", tension = "tension"),
  posthoc = FALSE
)
draw_volcano_plot(estimate_significance(fact, log2fc_cutoff = 0.1),
                  main = "warpbreaks")

## One panel per term of a crossed design: wool, tension, wool:tension
draw_volcano_plot(estimate_significance(fact, by = "term", log2fc_cutoff = 0.1),
                  main = "warpbreaks, by term")

}
\seealso{
\code{\link[=estimate_significance]{estimate_significance()}}, whose output is the only argument this
function needs.
}
