% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_corrplot.R
\name{draw_corrplot}
\alias{draw_corrplot}
\title{Draw a correlation matrix, with the cells that failed the test left blank}
\usage{
draw_corrplot(
  cor_matrix,
  method = NULL,
  pvalue = NULL,
  use_adjusted = TRUE,
  sig_level = 0.05,
  cluster = TRUE,
  hclust_method = c("average", "complete", "ward.D2"),
  zlim = c(-1, 1),
  anno = TRUE,
  main = NULL,
  cex.anno = 1,
  cex.axis = 0.9,
  cex.main = 1.5,
  cex.legend = 1.2,
  ...
)
}
\arguments{
\item{cor_matrix}{The result of \code{\link[=summarize_association_stats]{summarize_association_stats()}}, or a
correlation matrix on its own. A matrix must be square and symmetric, with
the features as its dimnames, and hold at least two of them.}

\item{method}{Which coefficient to draw when \code{cor_matrix} is a result, naming
one of the slots it holds. \code{NULL} takes the first method it was computed
with. It must be \code{NULL} when \code{cor_matrix} is a matrix, there being only one
matrix to draw.}

\item{pvalue}{Matrix of p-values laid out like \code{cor_matrix}, used only when
\code{cor_matrix} is a matrix. When \code{cor_matrix} is a result the p-values come
from the same slot as the coefficients, so this must be left \code{NULL}.}

\item{use_adjusted}{Whether to read \code{adj_pvalue} rather than \code{pvalue} out of
the result. Ignored when \code{pvalue} is supplied directly.}

\item{sig_level}{Largest p-value a cell may have and still be drawn. Cells
above it are blanked. The diagonal is never blanked, a feature not being
tested against itself.}

\item{cluster}{Whether to reorder the features by clustering them. \code{FALSE}
keeps the order they arrive in.}

\item{hclust_method}{Linkage handed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{zlim}{Numeric length-2 range the colours span. The default is the range
a correlation can take, so that the same colour means the same strength from
one plot to the next.}

\item{anno}{Whether to write each coefficient on its cell, rounded to two
decimal places by \code{\link[=draw_heatmap]{draw_heatmap()}}. Blanked cells stay blank.}

\item{main}{Plot title.}

\item{cex.anno}{Character expansion for those cell labels, relative to the
smaller of the feature axis label sizes.}

\item{cex.axis, cex.main, cex.legend}{Character expansion for the axis labels,
title and colour key, passed to \code{\link[=draw_heatmap]{draw_heatmap()}}.}

\item{...}{Passed to \code{\link[=draw_heatmap]{draw_heatmap()}}. The arguments this function decides
(\code{data}, \code{group}, \code{group_lv}, \code{scale}, \code{cluster_feats}, \code{cluster_samples},
\code{anno}, \code{cex.anno}, \code{cex.axis}, \code{cex.main}, \code{cex.legend}) cannot be given
again.}
}
\value{
A list, invisibly: everything \code{\link[=draw_heatmap]{draw_heatmap()}} returns, and beside it

\describe{
\item{\code{corr}}{The matrix as it was drawn, in the drawn order and with the
blanked cells \code{NA}.}
\item{\code{pvalue}}{The p-values in that same order, or \code{NULL}.}
\item{\code{order}}{The permutation of the input the clustering chose.}
\item{\code{hclust}}{The \code{\link[stats:hclust]{stats::hclust()}} object behind it, or \code{NULL} when the
features were not clustered.}
\item{\code{n_masked}}{How many cells were blanked.}
}
}
\description{
A corrplot: one cell per pair of features, coloured by the coefficient on a
fixed -1 to 1 scale, with both axes in one order so that the diagonal runs
corner to corner and blocks of features that move together sit next to each
other. Given the p-values as well, the pairs that did not clear \code{sig_level}
are drawn as blank cells, so that what is left coloured is what there is
evidence for.
}
\details{
\code{\link[=draw_heatmap]{draw_heatmap()}} draws it. This function decides what it is handed: the matrix
unscaled, the colour range fixed at the range a correlation can take, one
clustering shared by the two axes, and the blanking applied afterwards.

The distance the clustering runs on is \code{1 - cor()}, the same one
\code{\link[=cluster_hclust]{cluster_hclust()}} and \code{\link[=draw_heatmap]{draw_heatmap()}} mean by \code{dist_method = "correlation"}, so a corrplot and a heatmap of the same features group them
the same way. It is computed once and both axes are permuted by it. A matrix
holding an \code{NA}, which is what a feature with no variance leaves behind, has
no distance for that feature, and rather than fail the features are left in
their input order with a message saying so.

Blanking happens after the clustering rather than before it. A cell removed
for its p-value would otherwise change the tree, and the picture would no
longer be the matrix the reader is being shown.

A cell whose p-value is \code{NA}, a pair that could not be tested, is left as it
arrived rather than blanked: there is no evidence against it either, and the
coefficient beside it is usually already \code{NA}.

The matrix is symmetric, so the transpose \code{\link[=draw_heatmap]{draw_heatmap()}} takes on the way in
leaves it unchanged and the features come out on both axes.
}
\examples{
feats <- c("mpg", "cyl", "disp", "hp", "drat", "wt")
res <- summarize_association_stats(mtcars, feats, methods = "pearson")

## Every pair, with the features clustered so that the blocks sit together
drawn <- draw_corrplot(res, main = "mtcars")
drawn$order

## Only the pairs that cleared the adjustment at 1\%
draw_corrplot(res, sig_level = 0.01, cex.anno = 0.8, cex.axis = 0.7)

## A bare matrix, in the order it arrives and with nothing to blank
draw_corrplot(stats::cor(mtcars[feats]), cluster = FALSE)

}
\seealso{
\code{\link[=summarize_association_stats]{summarize_association_stats()}}, which produces what this draws, and
\code{\link[=draw_heatmap]{draw_heatmap()}}, which draws it.
}
