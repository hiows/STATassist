% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{coef.sa_fit}
\alias{coef.sa_fit}
\alias{summary.sa_fit}
\alias{predict.sa_fit}
\title{Coefficients, summary and predictions from the model inside a fit}
\usage{
\method{coef}{sa_fit}(object, ...)

\method{summary}{sa_fit}(object, ...)

\method{predict}{sa_fit}(object, newdata = NULL, type = "raw", ...)
}
\arguments{
\item{object}{The \code{fit} element of a \code{\link[=fit_linear_regression]{fit_linear_regression()}},
\code{\link[=fit_logistic_regression]{fit_logistic_regression()}}, \code{\link[=fit_elastic_net]{fit_elastic_net()}}, \code{\link[=fit_rf]{fit_rf()}} or \code{\link[=fit_svm]{fit_svm()}}
result.}

\item{...}{Passed on to the method of the underlying fit.}

\item{newdata}{Rows to predict as \code{caret} takes them, which for a penalized fit
or a machine is the design matrix rather than the predictor frame, or \code{NULL}
for the rows the model was fitted on. \code{\link[=predict.sa_model]{predict.sa_model()}} takes the frame.}

\item{type}{\code{"raw"} for the prediction as \code{caret} gives it, a fitted value or
a class label; \code{"response"} for the same thing on the scale of the outcome,
which for a classification is the probability of \code{outcome_lv[2]}; or
\code{"prob"} for one column per class.}
}
\value{
From \code{coef()}, a named numeric vector of coefficients, \code{NA} for a term
the fit could not estimate and exactly 0 for one a penalty dropped. From
\code{summary()}, the \code{summary.lm} or
\code{summary.glm} object of the underlying fit. The \code{Call} it prints belongs to
\code{caret} rather than to you: \code{.outcome ~ .} on the predictor frame for a
linear model, and empty for a logistic one, which \code{caret} fits without a
formula. What was fitted to what is what \code{print()} on the result says.

From \code{predict()}, whatever \code{\link[caret:predict.train]{caret::predict.train()}} returns for that \code{type},
except that \code{type = "response"} on a classification gives an unnamed numeric
vector of probabilities, one per row of \code{newdata}.
}
\description{
The \code{fit} element of an \code{sa_model} is the engine object, kept so that the
model can \code{\link[=predict]{predict()}}. These methods make the rest of what anyone does with a
fitted model work on it as well: \code{coef()} and \code{summary()} read through to the
\code{\link[stats:lm]{stats::lm()}} or \code{\link[stats:glm]{stats::glm()}} fit that \code{\link[caret:train]{caret::train()}} built, so what comes
back is what the same call on that model would give.
}
\details{
The numbers are also in \verb{$coefficients}, as a data.frame with the confidence
interval beside them and, for a logistic regression, the odds ratio, and that
table is what \code{\link[=coef.sa_model]{coef.sa_model()}} gives for the result as a whole. These methods
are for when an \code{lm}-shaped answer is what is wanted instead: a named vector to
index, or the summary layout that is already familiar.

On a \code{\link[=fit_elastic_net]{fit_elastic_net()}} fit, \code{coef()} is the coefficients at the penalty that
was chosen, which is the model the result describes and the one \code{predict()}
predicts from, rather than the whole \code{lambda} path \code{glmnet} keeps. \code{summary()}
has nothing of the same shape to return there, since a penalized fit has no
standard errors and no residual degrees of freedom to test against; what it
does have is in \verb{$coefficients} and \verb{$fit_stats}.

A \code{\link[=fit_rf]{fit_rf()}} or \code{\link[=fit_svm]{fit_svm()}} fit has neither. \code{coef()} on one is an error naming
the importance table to read instead, rather than the \code{NULL} \code{coef()} on a
\code{randomForest} object gives or the S4 indexing error it gives on a \code{ksvm} one,
and \code{summary()} reaches a model with no summary method of its own.
\code{print(fit$fit)} is the readable answer for both, and everything the result
reports is in \verb{$coefficients} and \verb{$fit_stats}.

\code{predict()} here takes \code{newdata} as \code{caret} prepared it rather than as the fit
received it, which for \code{\link[=fit_elastic_net]{fit_elastic_net()}} and \code{\link[=fit_svm]{fit_svm()}} is a design matrix
and not a data frame of predictors. \code{\link[=predict.sa_model]{predict.sa_model()}} is the method that
takes the rows
themselves, since the result is what knows which columns they were and what
levels its factors had.

\code{predict()} is otherwise \code{caret}'s, with one word added. \code{\link[caret:train]{caret::train()}} knows
\code{type = "raw"} for the prediction itself and \code{type = "prob"} for the class
probabilities, and refuses anything else; \code{"response"}, which is what
\code{\link[stats:glm]{stats::glm()}} and \code{\link[stats:lm]{stats::lm()}} call the prediction on the scale of the
outcome, is therefore an error on an object that is plainly a logistic
regression. It is accepted here and means the same thing in both models: the
predicted value for a linear regression, and for a logistic one the
probability of \code{outcome_lv[2]}, the class every coefficient and odds ratio in
the result already describes. The probability is \code{caret}'s own, the second
column of \code{type = "prob"}, so nothing is computed twice.
}
\examples{
fit <- fit_linear_regression(mtcars, outcome = "mpg",
                             predictors = c("wt", "hp"), cv = FALSE)
coef(fit$fit)
summary(fit$fit)

## The same numbers the result reports in its own table.
fit$coefficients

## On a classification, `type = "response"` is the probability of the second
## level of `outcome_lv`, the one the coefficients describe.
iris2 <- iris[iris$Species != "setosa", ]
clf <- fit_logistic_regression(iris2, outcome = "Species",
                               predictors = "Petal.Length",
                               outcome_lv = c("versicolor", "virginica"),
                               cv = FALSE)
head(predict(clf$fit, newdata = iris2, type = "response"))

## A penalized fit answers with the coefficients at the chosen penalty, the
## ones `$coefficients` reports, rather than with the whole lambda path.
pen <- fit_elastic_net(mtcars, outcome = "mpg", penalty = "lasso",
                       lambda = 0.5, cv = FALSE)
coef(pen$fit)

}
