% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_roc_curve.R
\name{draw_roc_curve}
\alias{draw_roc_curve}
\title{Draw the ROC curves of an evaluated classification}
\usage{
draw_roc_curve(
  performance_result,
  models = NULL,
  anno_auc = FALSE,
  chance = TRUE,
  dark = FALSE,
  col = NULL,
  lwd = 2,
  lty = 1,
  legend_pos = "bottomright",
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  cex.axis = 1.2,
  cex.lab = 1.3,
  cex.main = 1.3,
  cex.legend = 1.1,
  cex.anno = NULL
)
}
\arguments{
\item{performance_result}{A classification evaluation, as returned by
\code{\link[=evaluate_classification_models]{evaluate_classification_models()}}.}

\item{models}{Which models to draw and in what order, or \code{NULL} for all of
them in the order the evaluation holds, which puts the baseline first.}

\item{anno_auc}{Whether to add each model's AUC to its legend entry.}

\item{chance}{Whether to draw the chance diagonal.}

\item{dark}{Whether to draw on a dark background.}

\item{col}{One colour, or one per drawn model. \code{NULL} takes them from
\code{hcl.colors(n, "Dark 2")}.}

\item{lwd}{Width of the curves.}

\item{lty}{One line type, or one per drawn model.}

\item{legend_pos}{Where to put the legend, or \code{NULL} for no legend.}

\item{xlab, ylab, main}{Axis and figure labels. \code{NULL} builds them from the
evaluation.}

\item{cex.axis, cex.lab, cex.main, cex.legend, cex.anno}{Relative text sizes.
\code{cex.legend} sizes the model-name legend when \code{anno_auc = FALSE}.
\code{cex.anno} sizes it once each entry carries an AUC; \code{NULL} matches
\code{cex.legend}.}
}
\value{
The rows of \verb{$metrics} that were drawn, invisibly.
}
\description{
The picture of an \code{\link[=evaluate_classification_models]{evaluate_classification_models()}} result: one curve per
model, all on the rows every model was scored on, with the chance diagonal to
read them against.
}
\details{
The curves are \verb{$curves}, the operating points the evaluation already
computed, rather than anything recomputed here, so the picture and the \code{auc}
column of \verb{$metrics} describe the same curve. Consecutive points are joined
by straight lines, which is what makes the area under the drawn curve the
\code{auc} beside it: a run of tied predictions cannot be separated by any
threshold and the curve crosses it diagonally.

Every model is drawn against the same rows, since that is what the evaluation
scored them on, so two curves crossing is a statement about the models rather
than about which rows each of them managed.
}
\examples{
iris2 <- iris[iris$Species != "setosa", ]
iris2$Species <- factor(iris2$Species)
train <- iris2[c(1:35, 51:85), ]
test <- iris2[c(36:50, 86:100), ]

full <- fit_logistic_regression(train, outcome = "Species", cv = FALSE)
petal <- fit_logistic_regression(train, outcome = "Species",
                                 predictors = "Petal.Width", cv = FALSE)
res <- evaluate_classification_models(full, list(petal_only = petal),
                                      newdata = test)

draw_roc_curve(res, anno_auc = TRUE)

}
\seealso{
\code{\link[=evaluate_classification_models]{evaluate_classification_models()}} for the result this draws, and
\code{\link[=draw_prediction_plot]{draw_prediction_plot()}} for the regression counterpart.
}
