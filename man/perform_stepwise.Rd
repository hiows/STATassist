% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform_stepwise.R
\name{perform_stepwise}
\alias{perform_stepwise}
\title{Stepwise feature selection by information criteria}
\usage{
perform_stepwise(
  data,
  outcome,
  predictors = NULL,
  outcome_lv = NULL,
  control_label = outcome_lv[1],
  model = c("linear", "logistic"),
  criterion = c("AIC", "BIC"),
  direction = c("backward", "both", "forward")
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per observation.
Typically the training half of a \code{\link[=split_data]{split_data()}} result.}

\item{outcome}{The outcome, either the name of a column of \code{data} or a vector
with one entry per row.}

\item{predictors}{Candidate column names, or \code{NULL} for every column of \code{data}
except the outcome. Numeric, logical, factor and character columns are all
accepted; a factor is one candidate however many dummy columns it becomes.}

\item{outcome_lv}{For a two-class outcome, the two classes with the reference
first. \code{NULL} sorts them, which puts \code{"control"} before \code{"treated"} and \code{0}
before \code{1}. Naming it is also what tells this function that a numeric column
of zeroes and ones is two classes rather than two numbers.}

\item{control_label}{The reference class on its own, for when the other one
needs no saying. Defaults to \code{outcome_lv[1]}, so a call that names one of the
two names the reference either way; naming both and disagreeing is an error.}

\item{model}{What is fitted at every step: \code{"linear"} for a continuous outcome
or \code{"logistic"} for a two-class one.}

\item{criterion}{Which criterion the moves are judged by, \code{"AIC"} or \code{"BIC"}.
The search is the same one either way; what changes is the charge per
parameter, 2 against \code{log(n)}.}

\item{direction}{\code{"backward"} starts at every candidate and only drops,
\code{"forward"} starts at the intercept and only adds, and \code{"both"} starts at
every candidate and may put a dropped term back.}
}
\value{
An object of class \code{sa_selection}, a plain list.

\describe{
\item{\code{analysis}}{\code{"stepwise"}.}
\item{\code{candidates}}{The predictors that were offered, most important first.
This is the row order \code{ranking} follows, and what \code{terms} is to a model.}
\item{\code{design}}{What the search saw: the \code{outcome} and its \code{outcome_type},
\code{outcome_lv} with \code{n_events} and \code{event_rate} for a classification, the
row counts \code{n_obs}, \code{n_used} and \code{n_dropped}, the \code{predictors} in the
order they arrived, any \code{dropped_predictors}, and \code{predictor_lv} for those
that are factors.}
\item{\code{parameters}}{The choices as they were used: \code{model}, \code{criterion},
\code{maximize}, which is \code{FALSE} because a smaller criterion is a better
model, \code{k}, the charge per parameter it levied, and \code{direction}.}
\item{\code{selected}}{The predictors of the model the search stopped at, most
important first. These are the names to hand to \verb{predictors =} in a
\verb{fit_*()} call.}
\item{\code{ranking}}{One row per candidate: \code{candidates}, the \code{estimate} it was
ranked by, its \code{rank}, and whether it was \code{selected}. The estimate is what
leaving that one predictor out of the selected model costs the criterion,
so it is positive for a predictor worth keeping and negative for one worth
leaving out.}
\item{\code{profile}}{One row per step of the path: \code{n_vars}, both \code{AIC} and
\code{BIC} of the model at that step, the \code{step} that was taken to reach it,
and \code{chosen}, which is \code{TRUE} on the last row.}
\item{\code{resampling}}{\code{NULL}. Nothing was resampled.}
\item{\code{engine}}{What ran the search, including \code{importance}, the name of
what \code{ranking$estimate} measures.}
\item{\code{fit}}{The \code{\link[stats:step]{stats::step()}} result, which is the selected \code{lm} or \code{glm}
with the path attached as \verb{$anova}. This is the slot that is not portable;
dropping it leaves an object that writes out as JSON.}
\item{\code{metadata}}{Package version, R version, platform and timestamp.}
}
}
\description{
Runs a stepwise search: the model is refitted with each candidate term taken out
or put in, the move that lowers AIC or BIC the most is taken, and the search
stops when no single move lowers it any further. What comes back is the
predictors of the model it stopped at, the path it walked to get there, and what
each candidate is worth to that model, so a set of four predictors can be read
against what the other six would have cost.
}
\details{
The input is the wide format the model functions take, \strong{one row per observation
with one column as the outcome}, and is normally the training half of a
\code{\link[=split_data]{split_data()}} result. Selecting on data a model is later scored on is how a
selection flatters itself; see the details.
}
\section{What the criterion charges}{
Both criteria are the fit's own log likelihood with a charge levied per
parameter, and they differ only in the size of the charge: \code{"AIC"} levies 2 and
\code{"BIC"} levies \code{log(n)}, so past seven observations BIC charges more and keeps
fewer predictors. Smaller is better for both, which is why \code{maximize} is \code{FALSE}
and is reported rather than asked for. The charge as it was used is
\code{parameters$k}.

Whichever one searches, \code{profile} reports both at every step, so a path chosen
by AIC can be read against what BIC would have said about the same models. They
are \code{\link[stats:AIC]{stats::AIC()}}'s and \code{\link[stats:BIC]{stats::BIC()}}'s own numbers, the ones
\code{\link[=fit_linear_regression]{fit_linear_regression()}} reports in \code{fit_stats}, so a step of the path and a
fitted model are the same number. \code{\link[stats:step]{stats::step()}} itself searches on
\code{\link[stats:extractAIC]{stats::extractAIC()}}'s scale, which for a linear model differs from
\code{\link[stats:AIC]{stats::AIC()}}'s by a constant that is the same for every model on \code{n} rows;
the path is therefore ordered identically and only the printed values differ.

A criterion is comparable across models only when they were fitted to the same
rows, which is what the listwise deletion below is for. It is not comparable
across outcomes, across transformations of an outcome, or between a linear and a
logistic fit, so the numbers in \code{profile} say which of these models to prefer
and nothing more.
}

\section{What the search is not}{
The criterion is computed on the rows the model was fitted to, and the model was
kept because it scored best on them. Three things follow, and none of them is a
fault of the implementation:

\describe{
\item{The criterion is not a validation}{Score the selection on the test half
of \code{\link[=split_data]{split_data()}}, which the search never saw. That is the only number here
that is a claim about new rows, and it is why this function has no \code{cv}
argument: cross-validating a penalised likelihood computed on the training
rows would resample the wrong quantity.}
\item{The p-values of the selected model are no longer honest}{A coefficient
kept because it was significant enough to survive is being tested against a
null it was already screened on, so the p-value \code{\link[=fit_linear_regression]{fit_linear_regression()}}
reports for it on the same rows reads smaller than it is. Refit on held-out
rows, or read the fit as a description of these rows rather than as a test.}
\item{The path is greedy}{One move is taken at a time, so a pair of predictors
that is worth keeping only together can be dropped one at a time and never
come back. \code{direction = "both"} reconsiders a dropped term at every later
step, which is what it is for; nothing short of fitting every subset removes
the problem entirely.}
}

\code{\link[=fit_elastic_net]{fit_elastic_net()}} answers the same question without a path at all, by
shrinking a coefficient to exactly zero, and \code{\link[=perform_rfe]{perform_rfe()}} answers it with a
resampled score rather than with a penalty.
}

\section{Where the search starts and which way it moves}{
\code{"backward"} starts at every candidate and only drops. \code{"forward"} starts at the
intercept and only adds. \code{"both"} starts at every candidate and may add a term
back after dropping it, so its path can visit the same size twice; that is the
one direction whose \code{profile} is not a ladder.

Every direction is bounded by the same two models: the intercept alone below and
all of \code{predictors} above. A search that walks back to the intercept has kept
nothing, which is an answer — no candidate pays for itself at this charge — but
not one this contract can carry, since \code{selected} would be empty. It is an error
saying so rather than a result with a hole in it.
}

\section{Which model does the searching}{
\code{model} names what is fitted at every step, and the outcome has to agree with
it: \code{"linear"} is a continuous outcome and \code{"logistic"} a two-class one. A
disagreement is an error naming the model that would have fitted, rather than a
silently different analysis. There is no \code{"rf"} here, unlike \code{\link[=perform_rfe]{perform_rfe()}}: a
criterion is a likelihood with a charge against its parameter count, and a
forest has neither.

\code{outcome_lv} is read as it is everywhere else in this package: the first level
is the reference, so \code{outcome_lv = c("control", "case")} searches for the
predictors of \code{case}, and \code{control_label} names that first level on its own. For
the search itself the direction changes nothing, since the likelihood of a model
is the same number whichever class is called the reference; what the levels
decide is which class a later \code{\link[=fit_logistic_regression]{fit_logistic_regression()}} on \verb{$selected} reads.
}

\section{What is dropped before anything runs}{
The same listwise deletion the model functions use: rows missing the outcome or
any candidate go first, and \code{design$n_dropped} says how many went. Here it is
also what makes the search legible, since a criterion compares models only when
they were fitted to the same rows, and leaving the deletion to the engine would
have each step fitted to whatever its own terms happened to be complete on. A
candidate that takes a single value is left out with a message, since a column
with nothing in it cannot earn a parameter.
}

\section{Portability}{
Everything but \verb{$fit} is a scalar, a character vector, a named list or a
data.frame, so dropping that one slot leaves an object that writes out as JSON.
}

\examples{
## Six candidates and one continuous outcome. The path says what was dropped and
## when, and `$selected` is where the search stopped.
res <- perform_stepwise(mtcars, outcome = "mpg",
                    predictors = c("wt", "hp", "disp", "qsec", "drat", "carb"))
res
res$selected
res$profile

## The same search at a heavier charge per parameter keeps fewer predictors.
perform_stepwise(mtcars, outcome = "mpg",
             predictors = c("wt", "hp", "disp", "qsec", "drat", "carb"),
             criterion = "BIC")$selected

## The selection is a set of column names, so it goes straight back into a fit.
fit_linear_regression(mtcars, outcome = "mpg", predictors = res$selected,
                      cv = FALSE)$coefficients

## A two-class outcome, with the reference named on its own.
iris2 <- iris[iris$Species != "setosa", ]
perform_stepwise(iris2, outcome = "Species", control_label = "versicolor",
             model = "logistic")

}
\seealso{
\code{\link[=split_data]{split_data()}}, which defines the rows a selection should be run on,
\code{\link[=perform_rfe]{perform_rfe()}}, which chooses by a resampled score rather than by a penalty,
\code{\link[=fit_elastic_net]{fit_elastic_net()}}, which chooses by shrinking a coefficient to exactly zero,
and \code{\link[=fit_linear_regression]{fit_linear_regression()}} and \code{\link[=fit_logistic_regression]{fit_logistic_regression()}} for fitting the
predictors it kept.
}
