% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{plot.sa_performance}
\alias{plot.sa_performance}
\title{Draw an evaluation result}
\usage{
\method{plot}{sa_performance}(x, ...)
}
\arguments{
\item{x}{An evaluation, as returned by \code{\link[=evaluate_regression_models]{evaluate_regression_models()}} or
\code{\link[=evaluate_classification_models]{evaluate_classification_models()}}.}

\item{...}{Passed to whichever of the two is called.}
}
\value{
Whatever the function called returns, invisibly.
}
\description{
Dispatches on what was evaluated, since the two scenarios have different
pictures rather than one picture with a switch: a regression is drawn against
the outcome it predicted and a classification against the two classes it
ranked. Call \code{\link[=draw_prediction_plot]{draw_prediction_plot()}} or \code{\link[=draw_roc_curve]{draw_roc_curve()}} directly to reach
their own arguments by name.
}
\examples{
train <- mtcars[1:24, ]
test <- mtcars[25:32, ]
fit <- fit_linear_regression(train, outcome = "mpg",
                             predictors = c("wt", "hp"), cv = FALSE)
plot(evaluate_regression_models(fit, newdata = test))

}
\seealso{
\code{\link[=draw_prediction_plot]{draw_prediction_plot()}} and \code{\link[=draw_roc_curve]{draw_roc_curve()}}.
}
