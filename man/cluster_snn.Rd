% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_snn.R
\name{cluster_snn}
\alias{cluster_snn}
\title{Cluster by how many neighbours points have in common}
\usage{
cluster_snn(
  data,
  feats = NULL,
  cluster_scale = c("samples", "features"),
  center = TRUE,
  scale = TRUE,
  k = NULL,
  eps = NULL,
  min_pts = NULL
)
}
\arguments{
\item{data}{A data.frame or a matrix in wide format, one row per sample and one
column per feature.}

\item{feats}{Column names to cluster on, or \code{NULL} for every numeric column of
\code{data}.}

\item{cluster_scale}{Which margin becomes the points being clustered:
\code{"samples"}, the default, or \code{"features"}. \code{design$point_type} reports it.}

\item{center, scale}{Whether to centre each feature and divide it by its standard
deviation first. Both always apply to the \strong{columns of \code{data}}, whatever
\code{cluster_scale} is.}

\item{k}{How many nearest neighbours each point keeps. \code{NULL} derives it as the
square root of the number of points and reports what it came out as.}

\item{eps}{How many neighbours two points must \strong{share} before they are linked,
a whole number from 1 to \code{k}. Not a distance; see the details. \code{NULL} uses
\code{k / 2}.}

\item{min_pts}{How many links a point needs in that graph before it can be the
core of a cluster. \code{NULL} uses \code{k / 2}.}
}
\value{
An object of class \code{sa_cluster}, a plain list. The slots are
\code{\link[=cluster_hclust]{cluster_hclust()}}'s, with \code{analysis} \code{"snn"}, \code{fit} the
\code{\link[dbscan:sNNclust]{dbscan::sNNclust()}} object, and \code{design$n_clusters} and \code{design$n_noise} both
answers rather than arguments.
}
\description{
Builds a graph in which every point keeps its \code{k} nearest neighbours, links two
points that share at least \code{eps} of them, and grows a cluster out of every point
with at least \code{min_pts} such links. Points that never joined one are left as
noise. How many clusters there are is the answer rather than the question.
}
\details{
The input is the wide format the comparison functions take: \strong{one row per sample
and one column per feature}. Which margin of it becomes the thing being
clustered is \code{cluster_scale}, and \code{design$point_type} reports the answer.
}
\section{\code{eps} here counts neighbours, and in cluster_dbscan() it is a distance}{
The two functions have an argument of the same name meaning two different
things, which is the algorithms' doing rather than this package's:
\code{\link[dbscan:dbscan]{dbscan::dbscan()}} and \code{\link[dbscan:sNNclust]{dbscan::sNNclust()}} both call their threshold \code{eps}, and
renaming one of them here would make this package's documentation disagree with
the engine's. In \code{\link[=cluster_dbscan]{cluster_dbscan()}} \code{eps} is a radius in the units of the data
and any positive number is meaningful. Here it is a count of shared neighbours,
so it is a whole number between 1 and \code{k}, and a value of \code{k / 2} says "half of
what each of you considers close is the same points".
}

\section{Why this rather than a radius}{
One radius has to be right everywhere, so DBSCAN cannot find a tight cluster and
a loose one in the same call: the \code{eps} that reaches across the loose one merges
the tight one into its surroundings. Shared neighbours are relative, so a point
in a sparse region is still close to its own neighbours, and clusters of
different densities come out together.

The price is dimension. As the number of variables grows, distances concentrate
and neighbour lists start to overlap for no reason, so this method will report
structure that is an artefact of the width of the table. \code{clusters$silhouette}
is the check worth making, and agreement with \code{\link[=cluster_dbscan]{cluster_dbscan()}} on the same
points is worth more than either alone.
}

\section{Noise}{
A point that never joined a dense part of the graph gets cluster \code{0}, has no
silhouette and no row in \code{clusters}; \code{design$n_noise} is the count. Every point
being noise is a possible answer and means the overlap asked for is not there,
which is usually \code{eps} or \code{min_pts} being too large for the \code{k} in use.
}

\examples{
## Two blobs of different spread, which is the case a single radius handles
## badly and shared neighbours handle well.
set.seed(1)
blobs <- rbind(
  matrix(rnorm(80, mean = -6, sd = 0.4), ncol = 2),
  matrix(rnorm(80, mean =  6, sd = 2.5), ncol = 2)
)
colnames(blobs) <- c("x", "y")
res <- cluster_snn(blobs, scale = FALSE, k = 10)
res
table(res$assignments$cluster)

plot(blobs, col = res$assignments$cluster + 1L, pch = 16,
     main = "cluster 0, in black, is noise")

## On a planted two-group structure, with everything left to be derived.
sim <- simulate_two_groups(n_feats = 30, n_up = 5, n_down = 5, seed = 3)
by_samp <- cluster_snn(sim$args$data)
table(cluster = by_samp$assignments$cluster, group = sim$args$group)

}
\seealso{
\code{\link[=cluster_dbscan]{cluster_dbscan()}}, the other density method here, which measures
closeness with one radius rather than by shared neighbours.
}
