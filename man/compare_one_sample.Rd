% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_one_sample.R
\name{compare_one_sample}
\alias{compare_one_sample}
\title{Compare one sample against a hypothesised value}
\usage{
compare_one_sample(
  data,
  feats,
  mu = 0,
  p = 0.5,
  success = 1,
  alternative = c("two.sided", "less", "greater"),
  conf_level = 0.95,
  fc_mean = c("arith", "geom"),
  input_scale = c("raw", "log2"),
  p_adjust = "BH",
  diagnose = TRUE
)
}
\arguments{
\item{data}{A data.frame (or matrix) in wide format, one row per
observation and one column per feature.}

\item{feats}{Character vector of numeric column names in \code{data} to test.
One output row per entry.}

\item{mu}{Hypothesised value for the mean and the pseudo-median. A single
number applied to every feature, read on the same scale as \code{data}, so with
\code{input_scale = "log2"} it is a log2 value.}

\item{p}{Hypothesised proportion for \code{prop_test}, in \verb{(0, 1)}.}

\item{success}{The value counted as a success when a feature is binary.
Defaults to 1, so a 0/1 coded column needs nothing further.}

\item{alternative}{Direction of the alternative hypothesis, one of
\code{"two.sided"}, \code{"less"} or \code{"greater"}. \code{"greater"} tests whether the
sample exceeds \code{mu}, and every reported quantity follows that direction.}

\item{conf_level}{Confidence level for all reported intervals.}

\item{fc_mean}{Which centre the fold change divides \code{mu} into, \code{"arith"} for
the arithmetic mean or \code{"geom"} for the geometric mean. Defaults to
\code{"geom"} when \code{input_scale = "log2"} and to \code{"arith"} otherwise. Affects
the \code{effect} table only: the t-test is a test of the mean either way.}

\item{input_scale}{The scale \code{data} and \code{mu} arrive on, \code{"raw"} or \code{"log2"}.
On the log2 scale both are raised back through \code{2^x} before the ratio is
taken, so \code{fold_change} means what it does for raw input. This changes the
\code{effect} table only, never the tests. See \code{\link[=compare_two_groups]{compare_two_groups()}} for the
full account.}

\item{p_adjust}{Multiplicity adjustment applied across \code{feats} within each
test table, passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}. Use \code{"none"} to disable.}

\item{diagnose}{Logical. If \code{TRUE}, the normality check the t-test rests on
is attached as \verb{$diagnostics}.}
}
\value{
A \code{sa_comparison} object with the layout described in
\code{\link[=compare_two_groups]{compare_two_groups()}}, with two differences. \code{design} carries \code{mu},
\code{p} and \code{success} instead of \code{group_lv}, since there are no groups, and
\code{effect} holds \code{n_used}, \code{center} (the centre \code{fc_mean} selected), \code{mu},
\code{diff}, \code{fold_change} and \code{log2fc}, the ratio being that centre over \code{mu}.
Every column of \code{effect} is on the original measurement scale, so with
\code{input_scale = "log2"} its \code{center}, \code{mu} and \code{diff} are back-transformed
and differ from the same-named columns of \code{tests$t_test}, which stay on the
scale the tests ran on. \code{posthoc} is empty.

Every test table starts with \code{n_used} and carries \code{pval}, \code{pval_adj},
\code{lower_conf} and \code{upper_conf}. The remaining columns are:

\describe{
\item{\code{t_test}}{\code{center}, \code{mu}, \code{diff}, \code{stderr}, \code{t_stat}, \code{df} and
\code{cohens_d}, the mean difference over the sample standard deviation.}
\item{\code{wilcox_test}}{\code{hl_shift}, the Hodges-Lehmann pseudo-median, and
\code{v_stat}.}
\item{\code{prop_test}}{\code{n_success}, \code{proportion}, \code{p}, \code{diff}, \code{chi_sq},
\code{df} and \code{cohens_h}. The interval is a Wilson score interval, which
stays inside \verb{[0, 1]} where a Wald interval does not.}
}
}
\description{
Tests each feature against \code{mu} and returns a parametric, a rank-based and a
proportion result side by side, in the same shape \code{\link[=compare_two_groups]{compare_two_groups()}}
uses. There is no second group here, so the reference is a number the user
supplies rather than a set of observations.
}
\details{
\describe{
\item{\code{t_test}}{One-sample t-test on the mean.}
\item{\code{wilcox_test}}{One-sample Wilcoxon signed-rank test, with the
Hodges-Lehmann pseudo-median. It tests location without assuming
normality, but it does assume the distribution of \code{x - mu} is symmetric.}
\item{\code{prop_test}}{Score test of the proportion of successes against \code{p}.
It only applies to features that are binary, and features that are not
come back as \code{NA} rather than being silently coerced.}
}

Missing values are dropped per feature, so \code{n_used} varies between features
when the data are incomplete. Features that cannot be tested do not abort the
run: their row is filled with \code{NA} and all such features are reported
together in a single warning.
}
\section{Fold change against a hypothesised value}{

\code{fold_change} is \code{center / mu} and \code{log2fc} its base-2 logarithm, so both are
undefined when \code{mu} is zero, which is also its most common value. Both
columns are \code{NA} in that case rather than infinite, a message says so, and
\code{\link[=estimate_significance]{estimate_significance()}} will call every feature undecided. Reporting \code{Inf}
would read as an infinitely large increase when what actually happened is
that the question has no answer.

This cannot arise under \code{input_scale = "log2"}. There the reference is
\code{2^mu}, which is positive whatever \code{mu} is, and \code{mu = 0} simply means a
reference of 1. Under the default \code{fc_mean = "geom"} the whole row reduces to
\code{log2fc = mean(x) - mu}.
}

\examples{
## Are the iris measurements different from 3 cm?
res <- compare_one_sample(
  data  = iris,
  feats = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width"),
  mu    = 3
)
res
res$tests$t_test
res$tests$wilcox_test
res$effect

## A binary feature reaches the proportion test; a continuous one does not
## and comes back NA with a warning rather than being coerced.
cars <- mtcars
cars$am <- as.numeric(cars$am)
suppressWarnings(
  compare_one_sample(cars, c("am", "vs", "mpg"), mu = 0.5, p = 0.5)
)$tests$prop_test

## One-sided: is mileage above 20 mpg?
compare_one_sample(mtcars, "mpg", mu = 20,
                   alternative = "greater")$tests$t_test

}
\references{
Student (1908). The probable error of a mean. \emph{Biometrika}, 6(1), 1-25.

Wilcoxon, F. (1945). Individual comparisons by ranking methods.
\emph{Biometrics Bulletin}, 1(6), 80-83.

Wilson, E. B. (1927). Probable inference, the law of succession, and
statistical inference. \emph{Journal of the American Statistical Association},
22(158), 209-212.
}
\seealso{
\code{\link[=compare_two_groups]{compare_two_groups()}} for two groups and
\code{\link[=compare_multiple_groups]{compare_multiple_groups()}} for three or more.
}
