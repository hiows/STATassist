% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_prediction_plot.R
\name{draw_prediction_plot}
\alias{draw_prediction_plot}
\title{Draw predicted against observed for an evaluated regression}
\usage{
draw_prediction_plot(
  performance_result,
  models = NULL,
  type = c("auto", "overlay", "panel"),
  panel_nrow = NULL,
  points = TRUE,
  anno_corr = FALSE,
  anno_rsq = FALSE,
  anno_lm = FALSE,
  dark = FALSE,
  lim = NULL,
  col = NULL,
  lwd = 2,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  cex.axis = 1.2,
  cex.lab = 1.3,
  cex.main = 1.3,
  cex.legend = 1.1,
  cex.anno = NULL
)
}
\arguments{
\item{performance_result}{A regression evaluation, as returned by
\code{\link[=evaluate_regression_models]{evaluate_regression_models()}}.}

\item{models}{Which models to draw and in what order, or \code{NULL} for all of
them in the order the evaluation holds, which puts the baseline first.}

\item{type}{\code{"auto"}, \code{"overlay"} or \code{"panel"}, as described above.}

\item{panel_nrow}{Rows of panels under \code{type = "panel"}. \code{NULL} draws them in
one row.}

\item{points}{Whether to draw the predictions themselves. \code{FALSE} leaves the
calibration lines, which is what makes a crowded overlay readable.}

\item{anno_corr}{Whether to report each model's correlation beside its name.}

\item{anno_rsq}{Whether to report each model's held-out R-squared from
\verb{$metrics$r_squared}. This is \code{1 - SSE/SST} on the scored rows, not
\code{cor} squared, and the two are annotated separately for the same reason
the table carries both.}

\item{anno_lm}{Whether to report each model's calibration line as an
equation.}

\item{dark}{Whether to draw on a dark background.}

\item{lim}{Range of both axes, or \code{NULL} to span every value drawn.}

\item{col}{One colour, or one per drawn model. \code{NULL} takes them from
\code{hcl.colors(n, "Dark 2")}.}

\item{lwd}{Width of the calibration lines.}

\item{xlab, ylab, main}{Axis and figure labels. \code{NULL} builds them from the
evaluation.}

\item{cex.axis, cex.lab, cex.main, cex.legend, cex.anno}{Relative text sizes.
\code{cex.legend} sizes the model-name legend; \code{cex.anno} sizes the correlation,
R-squared and calibration labels drawn inside the plot area. \code{NULL} matches
\code{cex.legend}.}
}
\value{
The rows of \verb{$metrics} that were drawn, invisibly, carrying the
resolved \code{type} as a \code{"view"} attribute.
}
\description{
The picture of an \code{\link[=evaluate_regression_models]{evaluate_regression_models()}} result: each model's
predictions against the outcome they were predicting, with the identity line
to read them against and the calibration line to read the identity against.
}
\details{
Two lines are drawn in every panel. The dotted grey one is the identity,
where a prediction that was exactly right would lie. The solid coloured one
is \code{lm(predicted ~ observed)}, taken from the \code{calib_slope} and
\code{calib_intercept} of \verb{$metrics} rather than fitted again here, so the picture
and the table cannot drift apart. A slope under one is a model whose
predictions are squeezed towards their own mean, which is the usual shape of
a fit scored on rows it has not seen.

\code{type} decides how more than one model is shown. \code{"panel"} gives each its own
square, which is the default past one model because two clouds of points on
shared axes make a third cloud that belongs to neither. \code{"overlay"} puts them
all in one square, which is readable with \code{points = FALSE}, where what is
compared is the calibration lines. \code{"auto"} is \code{"overlay"} for a single model
and \code{"panel"} beyond that.

Both axes span the same range in every panel, taken over every model drawn,
so the panels are comparable and the identity line is the diagonal of the
square rather than an arbitrary chord.
}
\examples{
train <- mtcars[1:24, ]
test <- mtcars[25:32, ]
full <- fit_linear_regression(train, outcome = "mpg",
                              predictors = c("wt", "hp", "disp"),
                              cv = FALSE)
small <- fit_linear_regression(train, outcome = "mpg",
                               predictors = "wt", cv = FALSE)
res <- evaluate_regression_models(full, list(weight_only = small),
                                  newdata = test)

draw_prediction_plot(res, anno_lm = TRUE)
draw_prediction_plot(res, type = "overlay", points = FALSE,
                     anno_corr = TRUE, anno_rsq = TRUE)

}
\seealso{
\code{\link[=evaluate_regression_models]{evaluate_regression_models()}} for the result this draws, and
\code{\link[=draw_roc_curve]{draw_roc_curve()}} for the classification counterpart.
}
