% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical.R
\name{print.sa_categorical}
\alias{print.sa_categorical}
\title{Print a categorical comparison}
\usage{
\method{print}{sa_categorical}(x, alpha = 0.05, ...)
}
\arguments{
\item{x}{A categorical comparison, as returned by
\code{\link[=compare_categorical_groups]{compare_categorical_groups()}}.}

\item{alpha}{Threshold the reported verdict on each test is read at.}

\item{...}{Ignored, present for consistency with \code{\link[=print]{print()}}.}
}
\value{
\code{x} invisibly.
}
\description{
Summarises the table, the null hypothesis it was held against, the tests run
on it and the association measures, rather than printing the cell table.
\code{as.table()} gives the table as counts, \verb{$cells} the residuals and
\verb{$association} the measures with their intervals.
}
\examples{
compare_categorical_groups(
  data.frame(smoker = c(rep("y", 30), rep("n", 30)),
             grade  = c(rep(c("high", "low"), c(22, 8)),
                        rep(c("high", "low"), c(9, 21))))
)

}
