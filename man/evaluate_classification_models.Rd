% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_classification_models.R
\name{evaluate_classification_models}
\alias{evaluate_classification_models}
\title{Score fitted classifications on held-out rows}
\usage{
evaluate_classification_models(
  baseline_model,
  new_models = NULL,
  newdata,
  answer = NULL,
  outcome_lv = NULL,
  control_label = NULL,
  threshold = 0.5,
  conf_level = 0.95,
  baseline_label = "baseline"
)
}
\arguments{
\item{baseline_model}{The reference model, as returned by
\code{\link[=fit_logistic_regression]{fit_logistic_regression()}}, \code{\link[=fit_elastic_net]{fit_elastic_net()}}, \code{\link[=fit_rf]{fit_rf()}} or \code{\link[=fit_svm]{fit_svm()}}
with a two-class outcome. It is the first row of every table and what
\verb{$comparisons} is measured against.}

\item{new_models}{Named list of further models to hold against it, such as
\code{list(selected = fit_2, penalized = fit_3)}, or \code{NULL} to score the
baseline on its own. The names are what the tables and the plot legend call
the models, so an unnamed list is refused rather than numbered.}

\item{newdata}{The rows to score, typically the test half of a
\code{\link[=split_data]{split_data()}} result. Columns no model was fitted on are ignored.}

\item{answer}{The observed classes, either the name of a column of \code{newdata}
or a vector with one entry per row. \code{NULL} reads the column the models were
fitted to, which is the usual case.}

\item{outcome_lv}{The two classes, reference first, checked against the order
the models were fitted with rather than used to change it. \code{NULL} takes
theirs.}

\item{control_label}{The reference class on its own, checked the same way.}

\item{threshold}{Where to cut the predicted probability for \code{accuracy},
\code{sensitivity} and \code{specificity}. A row is called an event when its
probability is greater than or equal to this.}

\item{conf_level}{Confidence level of every interval in the result.}

\item{baseline_label}{What to call the baseline in the tables and the legend.}
}
\value{
An object of class \code{sa_performance}, a plain list of nine elements.

\describe{
\item{\code{analysis}}{\code{"classification_performance"}.}
\item{\code{models}}{Model names, the baseline first, in the row order every
table follows.}
\item{\code{design}}{What was scored: the \code{outcome} label, its type, its
\code{outcome_lv}, the \code{baseline} name, the row counts \code{n_obs}, \code{n_used} and
\code{n_dropped}, and \code{n_events}, how many scored rows were
\code{outcome_lv[2]}.}
\item{\code{parameters}}{\code{threshold} and \code{conf_level}.}
\item{\code{predictions}}{One row per model and scored row: \code{model}, \code{row}
(the position in \code{newdata}), \code{observed} (1 for \code{outcome_lv[2]}, 0 for
the reference) and \code{predicted} (the probability of \code{outcome_lv[2]}).}
\item{\code{metrics}}{One row per model: \code{n_used}, \code{n_events}, \code{auc} with its
interval, \code{brier}, and \code{accuracy}, \code{sensitivity} and \code{specificity} at
\code{threshold}.}
\item{\code{comparisons}}{One row per model other than the baseline, holding
\code{delta_auc}, \code{idi} and \code{nri}, each with its interval and p-value, and
the two class-wise components \code{nri_event} and \code{nri_nonevent}. Absent
when nothing was compared.}
\item{\code{curves}}{The ROC operating points, one row per model and distinct
predicted value: \code{model}, \code{threshold}, \code{sensitivity}, \code{specificity}.
Each curve opens at \code{threshold = Inf}, the cut above every prediction,
so that it starts at the corner where nothing is called an event.}
\item{\code{metadata}}{Package version, R version, platform and timestamp.}
}
}
\description{
Predicts one or more fitted two-class models on the same rows and reports how
each one discriminated, with three tests of each model against a baseline
where more than one was passed. Every model is read through
\code{\link[=predict.sa_model]{predict.sa_model()}} with \code{type = "response"}, so the five fitting functions
are interchangeable here and a logistic regression, a penalized one, a forest
and a machine are scored by the same arithmetic.
}
\details{
The rows are the intersection rather than the union. \code{predict()} on a model
answers \code{NA} for a row that is incomplete across \emph{that model's} predictors,
so models fitted on different predictor sets come back with different rows
filled in. Scoring each on whatever it managed would put two AUCs from two
samples in one table, and all three comparisons below are \strong{paired}
statistics that have no meaning at all across different rows. A row any model
cannot predict is therefore left out of all of them, with one message saying
how many went and why.

The direction is the fits'. \code{outcome_lv[2]} is the class
\code{predict(type = "response")} reports the probability of, so it is the class
every number here is about: \code{sensitivity} is measured against it, and an AUC
above 0.5 means the models rank it above the reference. \code{outcome_lv} and
\code{control_label} are read as a statement to be checked rather than as an
instruction, since a fitted model cannot be re-pointed after the fact, and
naming the other class is an error rather than a silent reversal. All the
models must agree on it too, which \code{\link[=evaluate_regression_models]{evaluate_regression_models()}} has no
counterpart of.

\verb{$metrics} reports what needs no threshold first. \code{auc} comes with the
interval its own DeLong standard error gives, which is a Wald interval on a
bounded quantity and can therefore run past 1 for a strong classifier;
\code{brier} is the mean squared distance between the probability and the outcome,
which an AUC is blind to, since a model that ranks perfectly and predicts
every event at 0.6 has an AUC of 1. \code{accuracy}, \code{sensitivity} and
\code{specificity} do need one, and it is \code{threshold}, recorded in \verb{$parameters}
so that the table says what it was measured at.

\verb{$comparisons} asks three different questions of the same pair of models,
which is why all three are reported rather than one being chosen.

\describe{
\item{\code{delta_auc}}{Whether the ranking improved, tested by DeLong's paired
test. Blind to any change that does not reorder rows.}
\item{\code{idi}}{How much further apart the two classes' predicted
probabilities moved, on the probability scale. Sees exactly the change an
AUC does not.}
\item{\code{nri}}{How often a probability moved the right way, counting
direction only, so a model that helps many rows slightly and hurts a few
badly scores well here and can score badly on the IDI. Category-free: no
risk strata are named, since their cut points are a clinical convention
rather than a property of the data.}
}

Every one of them is \code{new - baseline}, and every one is positive for a new
model that did better.
}
\examples{
## Two models of the same two-class outcome, scored on held-out rows.
iris2 <- iris[iris$Species != "setosa", ]
iris2$Species <- factor(iris2$Species)
train <- iris2[c(1:35, 51:85), ]
test <- iris2[c(36:50, 86:100), ]

full <- fit_logistic_regression(train, outcome = "Species", cv = FALSE)
petal <- fit_logistic_regression(train, outcome = "Species",
                                 predictors = "Petal.Width", cv = FALSE)

res <- evaluate_classification_models(full, list(petal_only = petal),
                                      newdata = test)
res
res$metrics

}
\references{
DeLong, E. R., DeLong, D. M. and Clarke-Pearson, D. L. (1988). Comparing the
areas under two or more correlated receiver operating characteristic curves:
a nonparametric approach. \emph{Biometrics}, 44(3), 837-845.

Pencina, M. J., D'Agostino, R. B., D'Agostino, R. B. and Vasan, R. S. (2008).
Evaluating the added predictive ability of a new marker: from area under the
ROC curve to reclassification and beyond. \emph{Statistics in Medicine}, 27(2),
157-172.
}
\seealso{
\code{\link[=evaluate_regression_models]{evaluate_regression_models()}} for the continuous counterpart,
\code{\link[=draw_roc_curve]{draw_roc_curve()}} for the picture of this result, and
\code{\link[=predict.sa_model]{predict.sa_model()}} for the call every model is read through.
}
