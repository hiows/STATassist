% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_kmeans.R
\name{cluster_kmeans}
\alias{cluster_kmeans}
\title{Cluster by moving centres until they stop}
\usage{
cluster_kmeans(
  data,
  feats = NULL,
  cluster_scale = c("samples", "features"),
  center = TRUE,
  scale = TRUE,
  n_clust = 2,
  n_start = 25,
  iter_max = 100,
  seed = NULL
)
}
\arguments{
\item{data}{A data.frame or a matrix in wide format, one row per sample and one
column per feature.}

\item{feats}{Column names to cluster on, or \code{NULL} for every numeric column of
\code{data}.}

\item{cluster_scale}{Which margin becomes the points being clustered:
\code{"samples"}, the default, or \code{"features"}. \code{design$point_type} reports it.}

\item{center, scale}{Whether to centre each feature and divide it by its standard
deviation first. Both always apply to the \strong{columns of \code{data}}, whatever
\code{cluster_scale} is. Scaling matters more here than anywhere else in this
family, since a mean is taken in the units the features arrived in.}

\item{n_clust}{How many centres to place. Unlike the two density methods, this
is the number of clusters that will come back.}

\item{n_start}{How many random starts to try, keeping the best. The default of
25 overrides \code{\link[stats:kmeans]{stats::kmeans()}}'s own default of 1.}

\item{iter_max}{Most iterations one start may take before it is abandoned.}

\item{seed}{Seed for the starts, or \code{NULL} to leave the random stream alone. The
stream is restored afterwards either way.}
}
\value{
An object of class \code{sa_cluster}, a plain list. The slots are
\code{\link[=cluster_hclust]{cluster_hclust()}}'s, with \code{analysis} \code{"kmeans"}, \code{fit} the
\code{\link[stats:kmeans]{stats::kmeans()}} object and \code{design$n_noise} always 0, since every point is
given to a centre.
}
\description{
Places \code{n_clust} centres, gives every point to its nearest one, moves each
centre to the mean of the points it was given, and repeats until nothing moves.
What comes back is one cluster label per point, beside the \code{\link[stats:kmeans]{stats::kmeans()}}
object holding the centres.
}
\details{
The input is the wide format the comparison functions take: \strong{one row per sample
and one column per feature}. Which margin of it becomes the thing being
clustered is \code{cluster_scale}, and \code{design$point_type} reports the answer.
}
\section{Reproducibility}{
The starting centres are random, so two calls can disagree. Two defences are on
by default and neither replaces the other. \code{n_start = 25} runs the whole thing
twenty-five times from different starts and keeps the one with the smallest
within-cluster sum of squares, which makes a bad local optimum unlikely rather
than impossible. \code{seed} makes the run exactly repeatable, and it restores the
random stream afterwards, so seeding this call does not quietly reseed whatever
the caller does next.

Neither is a guarantee that \code{n_clust} is the right number. k-means always
returns the number of groups it was asked for, and \code{clusters$silhouette} is
where to look for whether they are groups: a cluster near zero is one whose
members are about as close to another cluster as to their own.
}

\section{Which margin is clustered}{
\code{cluster_scale = "samples"}, the default, puts one point per row of \code{data}, and
\code{"features"} puts one point per column. See \code{\link[=cluster_hclust]{cluster_hclust()}}, which documents
the same argument and why transposing \code{data} by hand is a different analysis.
}

\section{What is dropped before anything runs}{
Rows that are not complete and finite across \code{feats} go before the centres are
placed, and \code{design$n_dropped} reports how many. A feature that takes a single
value cannot be scaled, so with \code{scale = TRUE} it is left out with a message and
named in \code{design$dropped_feats}.
}

\examples{
## Three species, and the clustering was not told there were three.
res <- cluster_kmeans(iris[1:4], n_clust = 3, seed = 1)
res
table(cluster = res$assignments$cluster, species = iris$Species)

## The centres are on `$fit`, in the scaled units the clustering ran in.
res$fit$centers

## On a planted two-group structure. The group was never shown to the engine.
sim <- simulate_two_groups(n_feats = 30, n_up = 5, n_down = 5, seed = 3)
by_samp <- cluster_kmeans(sim$args$data, n_clust = 2, seed = 1)
table(cluster = by_samp$assignments$cluster, group = sim$args$group)

## Asking for more groups than there are shows up in the silhouettes rather
## than in an error: k-means returns whatever number it was asked for.
cluster_kmeans(sim$args$data, n_clust = 5, seed = 1)$clusters

}
\seealso{
\code{\link[=cluster_hclust]{cluster_hclust()}}, which is told the same \code{n_clust} and returns the
tree it cut, and \code{\link[=cluster_dbscan]{cluster_dbscan()}}, which derives the count from the density
instead and may refuse to place a point.
}
