% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_regression_models.R
\name{evaluate_regression_models}
\alias{evaluate_regression_models}
\title{Score fitted regressions on held-out rows}
\usage{
evaluate_regression_models(
  baseline_model,
  new_models = NULL,
  newdata,
  answer = NULL,
  baseline_label = "baseline"
)
}
\arguments{
\item{baseline_model}{The reference model, as returned by
\code{\link[=fit_linear_regression]{fit_linear_regression()}}, \code{\link[=fit_elastic_net]{fit_elastic_net()}}, \code{\link[=fit_rf]{fit_rf()}} or \code{\link[=fit_svm]{fit_svm()}}.
It is the first row of every table and what \verb{$comparisons} subtracts.}

\item{new_models}{Named list of further models to hold against it, such as
\code{list(selected = fit_2, penalized = fit_3)}, or \code{NULL} to score the
baseline on its own. The names are what the tables and the plot legend call
the models, so an unnamed list is refused rather than numbered.}

\item{newdata}{The rows to score, typically the test half of a
\code{\link[=split_data]{split_data()}} result. Columns no model was fitted on are ignored.}

\item{answer}{The observed outcome, either the name of a column of \code{newdata}
or a vector with one entry per row. \code{NULL} reads the column the models were
fitted to, which is the usual case; a model fitted from a vector remembers
no name to look up and requires this.}

\item{baseline_label}{What to call the baseline in the tables and the legend.}
}
\value{
An object of class \code{sa_performance}, a plain list of eight elements.

\describe{
\item{\code{analysis}}{\code{"regression_performance"}.}
\item{\code{models}}{Model names, the baseline first, in the row order every
table follows.}
\item{\code{design}}{What was scored: the \code{outcome} label, its type, the
\code{baseline} name, and the row counts \code{n_obs}, \code{n_used} and \code{n_dropped}.}
\item{\code{parameters}}{Empty. Scoring a regression takes no choices, which
is the difference between this slot here and in a classification
evaluation.}
\item{\code{predictions}}{One row per model and scored row: \code{model}, \code{row}
(the position in \code{newdata}), \code{observed} and \code{predicted}.}
\item{\code{metrics}}{One row per model: \code{n_used}, \code{cor}, \code{r_squared}, \code{rmse},
\code{mae}, \code{bias}, \code{calib_slope} and \code{calib_intercept}.}
\item{\code{comparisons}}{One row per model other than the baseline, holding
\code{delta_cor}, \code{delta_r_squared}, \code{delta_rmse} and \code{delta_mae} as
\code{new - baseline}. Absent when nothing was compared.}
\item{\code{metadata}}{Package version, R version, platform and timestamp.}
}
}
\description{
Predicts one or more fitted regressions on the same rows and reports how each
one did, with the differences against a baseline where more than one model
was passed. Every model is read through \code{\link[=predict.sa_model]{predict.sa_model()}}, so the five
fitting functions are interchangeable here and a linear model, a penalized
one, a forest and a machine are scored by the same arithmetic.
}
\details{
The rows are the intersection rather than the union. \code{predict()} on a model
answers \code{NA} for a row that is incomplete across \emph{that model's} predictors,
so a baseline fitted on nine columns and a reduced model fitted on four
disagree about any row missing one of the extra five. Scoring each model on
whatever it managed would put two numbers from two samples in one table and
call their difference an improvement, so a row that any model cannot predict
is left out of all of them, and a single message reports how many went and
why. \code{design$n_used} and \code{design$n_dropped} record the outcome of that.

Every model must have been fitted to the same outcome, and to a continuous
one. A classification is refused by name and pointed at
\code{\link[=evaluate_classification_models]{evaluate_classification_models()}} rather than being scored by correlating a
probability against a class label, which would produce a number.

\verb{$metrics} reports \code{cor} and \code{r_squared} side by side because they answer
different questions and agree only for predictions that need no calibration.
\code{r_squared} is \code{1 - SSE/SST} on these rows, the fraction of the variance the
predictions actually removed, and it is negative for a model that does worse
than the mean of the outcome. \code{cor^2} is what that would be if the
predictions were first rescaled by a line fitted to these same rows, so the
gap between the two is what \code{calib_slope} and \code{calib_intercept} describe: the
line is \code{lm(predicted ~ observed)}, the same orientation
\code{\link[=draw_prediction_plot]{draw_prediction_plot()}} draws, so a slope under one is a model whose
predictions are compressed towards their own mean.

\code{rmse} and \code{mae} carry the names \code{\link[=fit_linear_regression]{fit_linear_regression()}} and the rest use
in \verb{$performance}, so a resampled score and a held-out score read in the same
unit under the same name. They are not the same number and are not meant to
be: one is measured inside the folds of the training data and the other on
rows drawn away from it.

\verb{$comparisons} is every column of \verb{$metrics} as \code{new - baseline}, so a
positive \code{delta_cor} and a \strong{negative} \code{delta_rmse} both say the new model
did better. There is no p-value beside them, since a difference of held-out
errors has no null this function is in a position to state.
}
\examples{
## Two models of the same outcome, scored on rows neither was fitted on.
train <- mtcars[1:24, ]
test <- mtcars[25:32, ]

full <- fit_linear_regression(train, outcome = "mpg",
                              predictors = c("wt", "hp", "disp"),
                              cv = FALSE)
small <- fit_linear_regression(train, outcome = "mpg",
                               predictors = "wt", cv = FALSE)

res <- evaluate_regression_models(full, list(weight_only = small),
                                  newdata = test)
res
res$metrics

}
\seealso{
\code{\link[=evaluate_classification_models]{evaluate_classification_models()}} for the two-class counterpart,
\code{\link[=draw_prediction_plot]{draw_prediction_plot()}} for the picture of this result, and
\code{\link[=predict.sa_model]{predict.sa_model()}} for the call every model is read through.
}
