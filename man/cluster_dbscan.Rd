% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_dbscan.R
\name{cluster_dbscan}
\alias{cluster_dbscan}
\title{Cluster by finding the dense regions}
\usage{
cluster_dbscan(
  data,
  feats = NULL,
  cluster_scale = c("samples", "features"),
  center = TRUE,
  scale = TRUE,
  eps = NULL,
  min_pts = NULL
)
}
\arguments{
\item{data}{A data.frame or a matrix in wide format, one row per sample and one
column per feature.}

\item{feats}{Column names to cluster on, or \code{NULL} for every numeric column of
\code{data}.}

\item{cluster_scale}{Which margin becomes the points being clustered:
\code{"samples"}, the default, or \code{"features"}. \code{design$point_type} reports it.}

\item{center, scale}{Whether to centre each feature and divide it by its standard
deviation first. Both always apply to the \strong{columns of \code{data}}, whatever
\code{cluster_scale} is. Scaling matters here because \code{eps} is one radius applied to
all of them at once.}

\item{eps}{The radius of a neighbourhood, in the units of the matrix being
clustered, or \code{NULL} to derive it as the radius reaching the \code{min_pts - 1}th
neighbour of 95\% of the points. See the details.}

\item{min_pts}{How many points must be within \code{eps} of a point, itself included,
before it can be the core of a cluster. \code{NULL} derives it from the number of
variables, capped at half the points; the derived value is reported in a
message.}
}
\value{
An object of class \code{sa_cluster}, a plain list. The slots are
\code{\link[=cluster_hclust]{cluster_hclust()}}'s, with \code{analysis} \code{"dbscan"}, \code{fit} the
\code{\link[dbscan:dbscan]{dbscan::dbscan()}} object, and \code{design$n_clusters} and \code{design$n_noise} both
answers rather than arguments.
}
\description{
Grows a cluster out of every point that has at least \code{min_pts} neighbours within
\code{eps} of it, joining the neighbourhoods that overlap, and leaves the points that
never fell into one as noise. How many clusters there are is the answer rather
than the question, and it can be none.
}
\details{
The input is the wide format the comparison functions take: \strong{one row per sample
and one column per feature}. Which margin of it becomes the thing being
clustered is \code{cluster_scale}, and \code{design$point_type} reports the answer.
}
\section{Noise is a result, not a failure}{
A point that never joined a neighbourhood gets cluster \code{0}, which is
\code{\link[dbscan:dbscan]{dbscan::dbscan()}}'s convention and this contract's. It has no silhouette, since
noise is not a cluster to be near or far from, and \code{clusters} has no row for it;
\code{design$n_noise} is the count. All points being noise is a possible answer and
means the density asked for is not present, which is usually \code{eps} being too
small or \code{min_pts} too large for the data.
}

\section{Choosing eps}{
\code{eps} is a radius in the units of the matrix being clustered, which is why there
is no default constant that could be right. Left as \code{NULL} it is derived from
the distances actually present: every point's distance to its \code{min_pts - 1}th
neighbour is collected, and \code{eps} is the 95th percentile of those. The rule
therefore reads as "assume about one point in twenty is noise, and set the
radius that leaves that many outside", which is a statement worth disagreeing
with rather than a number off a curve.

The quantity being read is the one \code{\link[dbscan:kNNdistplot]{dbscan::kNNdistplot()}} plots and the manual
procedure reads a knee off by eye. The knee is not what is taken, because
locating it arithmetically fails on a curve that rises gradually rather than
turning; the probe in \verb{Test/cursor_test/2026_08_15/} has the case. Either way it
is a heuristic: \code{parameters$eps_source} records whether the value was supplied
or derived, and a derived one is said out loud when it is used.

Note that this \code{eps} is a distance. \code{\link[=cluster_snn]{cluster_snn()}} has an argument of the same
name that counts shared neighbours, because the algorithm it comes from named it
that; the two are not comparable.
}

\section{Which margin is clustered}{
\code{cluster_scale = "samples"}, the default, puts one point per row of \code{data}, and
\code{"features"} puts one point per column. See \code{\link[=cluster_hclust]{cluster_hclust()}}, which documents
the same argument and why transposing \code{data} by hand is a different analysis.
}

\examples{
## Two well-separated blobs and a couple of strays. The count is not an
## argument, so finding two is the result rather than the setup.
set.seed(1)
blobs <- rbind(
  matrix(rnorm(60, mean = -3), ncol = 2),
  matrix(rnorm(60, mean =  3), ncol = 2),
  cbind(c(-12, 12), c(12, -12))
)
colnames(blobs) <- c("x", "y")
res <- cluster_dbscan(blobs, scale = FALSE)
res
table(res$assignments$cluster)

plot(blobs, col = res$assignments$cluster + 1L, pch = 16,
     main = "cluster 0, in black, is noise")

## On a planted two-group structure, with the radius left to be derived.
sim <- simulate_two_groups(n_feats = 30, n_up = 5, n_down = 5, seed = 3)
by_samp <- cluster_dbscan(sim$args$data)
by_samp$parameters$eps_source
table(cluster = by_samp$assignments$cluster, group = sim$args$group)

}
\seealso{
\code{\link[=cluster_snn]{cluster_snn()}}, the other density method here, which measures
closeness by how many neighbours two points share rather than by a radius, and
\code{\link[=cluster_kmeans]{cluster_kmeans()}}, which is told the number of groups and places every point.
}
