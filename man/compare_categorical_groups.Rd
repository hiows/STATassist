% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_categorical_groups.R
\name{compare_categorical_groups}
\alias{compare_categorical_groups}
\title{Test a contingency table with every applicable test at once}
\usage{
compare_categorical_groups(
  data,
  category_lv = NULL,
  control_label = NULL,
  paired = FALSE,
  conf_level = 0.95,
  correct = TRUE,
  exact = NULL,
  simulate_p_value = FALSE,
  n_resamples = 9999,
  max_levels = 20L,
  seed = NULL,
  diagnose = TRUE
)
}
\arguments{
\item{data}{A data.frame (or matrix) whose columns are the categorical
variables. Unlike the other comparison scenarios there is no \code{feats}
argument: the columns are what is tested rather than what is measured, so a
factor, a character vector, a logical or a 0/1 coded column all read as
categorical and are taken as their labels.}

\item{category_lv}{Named list giving the levels of each variable, with the
reference level first, or \code{NULL} to take every column of \code{data} with its
levels in sorted order. Naming it does three things sorting cannot: it picks
which columns take part, it fixes which level is the reference the odds ratio
is read against, and it drops the rows belonging to any level it leaves out.
Every level it names has to be present in \code{data}.}

\item{control_label}{The level to hold as the reference. For an independent
design this is one name per variable it points at, as a named list
(\code{list(smoker = "n", grade = "low")}) or as a named character vector
(\code{c(smoker = "n")}); the two shapes say the same thing, and a variable it
says nothing about is left as it arrived. A matched design has one level set
shared by every condition, so there it is a single level name. Defaults to
\code{NULL}, the first level of every variable, which under \code{category_lv = NULL}
is the first in sorted order.}

\item{paired}{Logical. If \code{TRUE}, the columns are read as repeated conditions
measured on the same row rather than as different variables. See "Which tests
run".}

\item{conf_level}{Confidence level for the association intervals and for the
conditional odds ratio of Fisher's exact test.}

\item{correct}{Whether to apply the continuity correction to the chi-square
approximation. \code{\link[stats:chisq.test]{stats::chisq.test()}} applies it to 2 x 2 tables only, so it
changes nothing on a larger one, and the exact branch of McNemar's test needs
none.}

\item{exact}{Read only by McNemar's test. \code{TRUE} for the exact binomial test on
the discordant pairs, \code{FALSE} for the chi-square approximation, or \code{NULL} to
take the exact test when there are fewer than 25 discordant pairs.
\code{parameters$exact} records which one ran.}

\item{simulate_p_value}{Read only by the tests of an independent design.
Replaces the chi-square approximation with a Monte Carlo p-value, and is the
way to get an answer out of Fisher's test on a large r x c table that cannot
be enumerated.}

\item{n_resamples}{How many tables the Monte Carlo p-value is taken over.}

\item{max_levels}{How many levels a variable may take before it is refused as a
category. \code{as.character()} turns a continuous measurement into one label per
observation, which makes a table with a cell per row and no test of
association to run on it; this is the ceiling that catches that rather than
letting it through. Checked against the levels actually used, so naming three
levels of a fifty-valued column in \code{category_lv} is a way through. Raise it
for a variable that genuinely has this many.}

\item{seed}{Seed for the Monte Carlo p-value, restored on exit, so a simulated
p-value is reproducible without the caller's random stream being disturbed.}

\item{diagnose}{Logical. If \code{TRUE}, the rule the reported approximation rests
on is attached as \verb{$diagnostics}.}
}
\value{
An \code{sa_categorical} object: a plain list, so it survives being written
out as JSON and read back in another language, with an S3 class on top that
supplies \code{\link[=print]{print()}}, \code{\link[=plot]{plot()}} and \code{\link[=as.table]{as.table()}}. Its elements are

\describe{
\item{\code{analysis}}{\code{"categorical_comparison"}.}
\item{\code{variables}}{The variable names, in the order \code{category_lv} fixed.}
\item{\code{design}}{\code{category_lv}, \code{null}, \code{paired}, \code{pairing}, \code{dim},
\code{row_var}, \code{col_var}, \code{n_used}, \code{n_dropped} and \code{n_incomplete}. \code{null} is
\code{"independence"}, \code{"symmetry"} or \code{"marginal_homogeneity"}, and it is what
\code{expected} and the residuals are read under.}
\item{\code{parameters}}{The analysis choices as used, so \code{exact} says which
branch of McNemar's test ran rather than what was passed.}
\item{\code{cells}}{One row per cell of the table: \code{row_level}, \code{col_level},
\code{observed}, \code{expected}, \code{residual}, \code{std_residual}, \code{prop_total},
\code{prop_row} and \code{prop_col}. This is the canonical form of the table, and
what \code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} reads. \code{as.table()} folds it back into a
\code{\link[=table]{table()}}, which is the shape to read it in rather than a second copy to
keep.}
\item{\code{tests}}{One one-row data.frame per test, named \code{chisq_test},
\code{fisher_test}, \code{mcnemar_test} or \code{cochran_q}, each carrying \code{n_used},
\code{statistic}, \code{df}, \code{pval}, \code{lower_conf} and \code{upper_conf}. There is no
\code{pval_adj}.}
\item{\code{test_info}}{The method \code{id}, a readable \code{label} and whether the test
is a matched one, per element of \code{tests}.}
\item{\code{association}}{One row per measure: \code{measure}, \code{estimate},
\code{lower_conf}, \code{upper_conf}. Which measures are defined depends on the
design and on the size of the table.}
\item{\code{diagnostics}}{The approximation rule this design rests on, or
\code{NULL}.}
\item{\code{metadata}}{\code{package_version}, \code{r_version}, \code{platform} and an
ISO-8601 \code{timestamp}.}
}
}
\description{
Crosses two categorical variables into a contingency table and returns an
asymptotic and an exact test of it side by side, together with the measures of
how strong the association is. Nothing is chosen on the user's behalf:
reporting both makes disagreement between them visible, which is the situation
where the choice between an approximation and an exact enumeration actually
matters.
}
\details{
This is the one scenario in the package with no feature axis. Every other
comparison asks its question once per numeric column and returns a table with
one row per column; a contingency table is asked about as a whole, so the
result carries the table itself in \verb{$cells} and one row per test in \verb{$tests}.
That is also why the result is not an \code{sa_comparison} and does not go through
\code{\link[=estimate_significance]{estimate_significance()}}: a volcano plot needs a signed effect per feature and
this scenario has no feature axis to carry one.
\code{\link[=estimate_categorical_significance]{estimate_categorical_significance()}} is the counterpart that reads it, one
verdict per cell. See "Why this is not a comparison result".

A row is dropped for one of two reasons, and the two are counted apart. A row
naming a level \code{category_lv} leaves out was measured and excluded, and appears
as \code{design$n_dropped}. A row missing a value in any variable was not measured,
and appears as \code{design$n_incomplete}; a table needs the whole row, so the
deletion is listwise.

The warning \code{\link[stats:chisq.test]{stats::chisq.test()}} raises about small expected counts is not
passed on. \verb{$diagnostics} states the same fact as a number, and the exact test
that does not need the approximation is already in the same result, so a
warning would be a less precise version of what is sitting next to it.

Fisher's exact test enumerates every table with the observed margins, and on a
large r x c one there are more of those than the algorithm's workspace holds.
That is a limit of the enumeration rather than a fault in the data, so
\verb{$tests$fisher_test$pval} comes back \code{NA} with a message saying so, instead of
the whole call failing and taking the chi-square result with it.
\code{simulate_p_value = TRUE} is the way to get an answer there.
}
\section{One test, three questions}{

The chi-square statistic answers what look like three different questions, and
the arithmetic is the same in each.

\describe{
\item{Independence}{Both variables were measured on one sample: is there any
association between them? This is the reading the default label uses.}
\item{Homogeneity}{One variable says which sample a row came from and the
other is what was measured: do the samples share the same distribution
over the categories? The sampling scheme differs, the null hypothesis is
stated differently, and the test is the same one.}
\item{Goodness of fit}{One variable against a set of expected proportions.
This one is genuinely different, being about a single margin rather than a
cross-classification, and it is not implemented here.}
}

Which of the first two a caller means is a fact about their sampling scheme,
not about their data, and the data cannot be inspected to find out. So this
function does not ask, and both readings are the same call.
}

\section{Which tests run}{

Decided by \code{paired} and by how many variables \code{category_lv} names, so there is
no argument naming a test.\tabular{lll}{
   design \tab null hypothesis \tab tests \cr
   independent, two variables \tab independence \tab Chi-square test of independence, Fisher's exact test \cr
   matched, two binary conditions \tab symmetry \tab McNemar's test \cr
   matched, three or more binary conditions \tab marginal homogeneity \tab Cochran's Q test \cr
}


A matched design reads the columns as repeated measurements of one thing on the
same row, so pairing is by row and there is no \code{id} argument. It also requires
the levels to be binary: the tests of symmetry that generalise McNemar's test
past two levels, Bowker's and Stuart-Maxwell's, are not implemented, and a
three-level matched design is an error naming them rather than a table quietly
collapsed to two levels.
}

\section{What the cells are expected to hold}{

\code{design$null} names the hypothesis the whole result is about, and
\verb{$cells$expected} is read under it. This matters because a contingency table
can be held against more than one null and the three designs above hold it
against three different ones.

Under \strong{independence} a cell is expected at the product of its margins over
the total. Under \strong{symmetry} it is expected at the average of it and its
transpose, so the diagonal is expected at exactly what it holds and carries no
residual: a pair that answered the same way under both conditions says nothing
about which condition raises the response. Under \strong{marginal homogeneity} every
condition is expected to show the pooled response rate, which on a
condition-by-response table is the same arithmetic as independence and a
different claim about the world.

The residuals follow the same hypothesis, which is what keeps them and the
p-value beside them talking about one thing. In a matched 2 x 2 the squared
Pearson residuals of the two discordant cells sum to \code{(b - c)^2 / (b + c)},
which is McNemar's uncorrected statistic exactly; the cell table and the test
are the same arithmetic read two ways. \code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} shades on those
residuals, so the picture is about the hypothesis the result tested rather than
about independence in every case.

\code{std_residual} is the exception. Its variance correction is derived for a table
held against its own margins and has no counterpart under symmetry, so it is
\code{NA} there rather than a number that looks referable to a standard normal and
is not.
}

\section{Why this is not a comparison result}{

\code{sa_new_comparison()} holds every table in a comparison to one row per feature,
and \code{\link[=estimate_significance]{estimate_significance()}} reads one \code{log2fc} per row off that axis. Neither
exists here. There is one table, so there is one p-value and no multiplicity to
adjust across, which is why no \code{pval_adj} column is carried. And an association
has no sign: \code{cramers_v} says how far the table sits from independence but not
in which direction, because past a 2 x 2 table there is no single direction to
name. On a 2 x 2 table there is one, and \code{odds_ratio} reports it.

The result is therefore an \code{sa_categorical}, which keeps the vocabulary of a
comparison -- \code{design}, \code{parameters}, \code{tests} beside \code{test_info}, \code{metadata} --
without claiming a contract it cannot meet. \code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} is what reads
it, in the place \code{\link[=draw_volcano_plot]{draw_volcano_plot()}} holds for the numeric scenarios.

Every sentence above is about the table. A \strong{cell} does have both axes:
\code{observed / expected} is a signed departure defined on a table of any shape,
and \code{std_residual} beside it is referred to a standard normal, so the cells of
one table are a family to adjust across.
\code{\link[=estimate_categorical_significance]{estimate_categorical_significance()}} is what reads that axis, and it is a
function of \verb{$cells} alone, so no slot is carried for it.
}

\section{Direction}{

Set once, by the order of the levels in \code{category_lv}, and \code{control_label} is
the second way of stating it. Every quantity that has a direction follows it.

\code{odds_ratio} is above 1 when the \strong{second} level of each variable occurs with
the second level of the other more often than independence would give, and
\code{phi_coefficient} is above zero in the same situation. Pointing
\code{control_label} at the other level of either variable inverts both, and
pointing it at the other level of both leaves them where they were. In a
matched design the second level is the response the conditions are compared on,
so \code{odds_ratio_paired} is above 1 when the later condition raises it.
}

\section{Which measures are defined}{

An independent table always reports \code{cramers_v} and
\code{contingency_coefficient}, and a 2 x 2 one adds \code{phi_coefficient} and
\code{odds_ratio}. A matched 2 x 2 reports \code{odds_ratio_paired},
\code{risk_difference_paired} and \code{cohens_g}, all three read off the discordant
cells. Three or more matched conditions report \code{kendalls_w}.

Only \code{odds_ratio} and the paired measures carry an interval. The others are
functions of the chi-square statistic whose sampling distribution has no
closed-form interval, so their \code{lower_conf} and \code{upper_conf} are \code{NA}: the
columns exist because every result table in the package carries them, not
because every number in them is finite.

Every measure is built from the \strong{uncorrected} chi-square statistic, whatever
\code{correct} was set to. Yates' correction is about referring a discrete statistic
to a continuous distribution, which is a statement about a p-value; letting it
into an effect size would make the reported strength of an association depend
on a choice made about its tail probability.
}

\examples{
## Both columns are categorical, so `data` is the whole input.
smoking <- data.frame(
  smoker = rep(c("y", "n"), each = 60),
  grade  = c(rep(c("high", "mid", "low"), c(10, 20, 30)),
             rep(c("high", "mid", "low"), c(30, 20, 10)))
)
res <- compare_categorical_groups(smoking)
res

## The table itself, and the cells that made the statistic what it is.
as.table(res)
res$cells[c("row_level", "col_level", "observed", "expected", "std_residual")]

## How strong the association is, which no test reports.
res$association

## `category_lv` picks the levels and their order, and drops the rest. Two
## levels of `grade` make a 2 x 2 table, which is where the odds ratio exists.
two_by_two <- compare_categorical_groups(
  smoking,
  category_lv = list(smoker = c("n", "y"), grade = c("low", "high"))
)
subset(two_by_two$association, measure == "odds_ratio")

## `control_label` restates the reference without the levels being retyped,
## and pointing it at one variable inverts the odds ratio.
flipped <- compare_categorical_groups(
  smoking,
  category_lv   = list(smoker = c("n", "y"), grade = c("low", "high")),
  control_label = c(smoker = "y")
)
subset(flipped$association, measure == "odds_ratio")

## A matched design: the columns are the same question asked twice, so pairing
## is by row, the null is symmetry rather than independence, and McNemar's test
## reads only the discordant pairs.
before_after <- data.frame(
  before = rep(c("pass", "fail"), c(20, 30)),
  after  = c(rep(c("pass", "fail"), c(18, 2)), rep(c("pass", "fail"), c(14, 16)))
)
matched <- compare_categorical_groups(before_after, paired = TRUE)
matched$design$null

## Which is what the cell table is read under: the diagonal is expected at
## exactly what it holds, so only the discordant cells carry a residual.
matched$cells[c("row_level", "col_level", "observed", "expected", "residual")]

## A simulated table hands over exactly the arguments this function takes.
sim <- simulate_categorical_groups(n_samples = 400, assoc = 0.4, seed = 1)
fit <- do.call(compare_categorical_groups, sim$args)
cbind(planted = sim$truth$cramers_v,
      estimated = fit$association$estimate[1])

}
\references{
Pearson, K. (1900). On the criterion that a given system of deviations from the
probable in the case of a correlated system of variables is such that it can be
reasonably supposed to have arisen from random sampling. \emph{Philosophical
Magazine}, 50(302), 157-175.

Fisher, R. A. (1935). The logic of inductive inference. \emph{Journal of the Royal
Statistical Society}, 98(1), 39-82.

McNemar, Q. (1947). Note on the sampling error of the difference between
correlated proportions or percentages. \emph{Psychometrika}, 12(2), 153-157.

Cochran, W. G. (1950). The comparison of percentages in matched samples.
\emph{Biometrika}, 37(3-4), 256-266.

Agresti, A. (2002). \emph{Categorical Data Analysis}, 2nd ed. Wiley.
}
\seealso{
\code{\link[=simulate_categorical_groups]{simulate_categorical_groups()}} for a table whose association is
known, \code{\link[=estimate_categorical_significance]{estimate_categorical_significance()}} to reduce the result to a
verdict per cell, \code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} to draw it, and
\code{\link[=compare_multiple_groups]{compare_multiple_groups()}} for the case where the measurement is numeric and
only the grouping is categorical.
}
