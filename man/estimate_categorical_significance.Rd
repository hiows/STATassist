% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_categorical_significance.R
\name{estimate_categorical_significance}
\alias{estimate_categorical_significance}
\title{Reduce a contingency table to significance verdicts}
\usage{
estimate_categorical_significance(
  categorical_comparison_result,
  by = c("cell", "table"),
  test = names(categorical_comparison_result$tests)[1],
  log2_lift_cutoff = 1,
  pval_cutoff = 0.05,
  adj_type = "BH",
  measure = "auto",
  effect_cutoff = NULL
)
}
\arguments{
\item{categorical_comparison_result}{A categorical comparison result, as
returned by \code{\link[=compare_categorical_groups]{compare_categorical_groups()}}. A numeric comparison is refused
and pointed at \code{\link[=estimate_significance]{estimate_significance()}}.}

\item{by}{\code{"cell"} for one verdict per cell of the table, \code{"table"} for one
verdict for the table as a whole. See "The two readings".}

\item{test}{Which test supplies the p-value of a table reading. One of
\code{names(categorical_comparison_result$tests)}, so \code{"chisq_test"} or
\code{"fisher_test"} for an independent design. Defaults to the first test the
design ran. Read only by \code{by = "table"}: a cell reading takes its p-value
from the cell's own standardized residual, which no test reports.}

\item{log2_lift_cutoff}{Minimum \code{abs(log2_lift)} required to call a cell
significant. Read only by \code{by = "cell"}. The default of 1 is a cell holding
twice, or half, what its null expected, and is deliberately the same number
\code{\link[=estimate_significance]{estimate_significance()}} uses. It is a strict demand of a contingency
table, where departures well under a doubling are ordinary; halve it to ask
of a cell what the default asks of a fold change.}

\item{pval_cutoff}{Largest p-value allowed for a verdict of significant. Read
by both readings, against \code{adj_pvalue} under \code{by = "cell"} and against the
test's own \code{pvalue} under \code{by = "table"}.}

\item{adj_type}{Multiplicity adjustment across the cells, one of
\link[stats:p.adjust.methods]{stats::p.adjust.methods}. Read only by \code{by = "cell"}. Unlike the \code{adj_type}
of \code{\link[=estimate_significance]{estimate_significance()}} this is the first adjustment rather than a
replacement for one, since a categorical result carries no adjusted column.
\code{"none"} tests the raw p-values.}

\item{measure}{Which row of \verb{$association} a table reading puts on its effect
axis, or \code{"auto"} to take the one the design defines. Read only by
\code{by = "table"}.}

\item{effect_cutoff}{Magnitude \code{measure} has to reach for a table reading to
call the table significant, or \code{NULL} to judge on the p-value alone. Read
only by \code{by = "table"}.}
}
\value{
An \code{sa_categorical_significance} object, a list of two elements:
\describe{
\item{\code{analysis_type}}{\code{"categorical_comparison"}.}
\item{\code{significance}}{With \code{by = "cell"}, a data.frame with one row per
cell and the columns \code{row_level}, \code{col_level}, \code{observed}, \code{expected},
\code{lift}, \code{log2_lift}, \code{std_residual}, \code{pvalue}, \code{adj_pvalue} and
\code{is_signif}. With \code{by = "table"}, a one-row data.frame with \code{measure},
\code{estimate}, \code{lower_conf}, \code{upper_conf}, \code{pvalue} and \code{is_signif}.}
}

The cutoffs, the reading, the null hypothesis and whichever of the test name
and the adjustment applies are attached to the data.frame as attributes, so
a table that has been passed around still says which rule produced it.

A cell table keys on \code{c("row_level", "col_level")}, the key
\code{categorical_comparison_result$cells} and
\code{simulate_categorical_groups()$truth_cell} also use, so a verdict merges with
either without renaming. Note that \code{truth_cell} carries a \code{lift} column of
its own, the planted one, so a merge of the two distinguishes them as
\code{lift.x} and \code{lift.y}.

This is deliberately not an \code{sa_significance}. Its columns are cells rather
than features and \code{log2_lift} rather than \code{log2fc}, so \code{\link[=draw_volcano_plot]{draw_volcano_plot()}}
refusing it is the point of the separate class rather than an omission.
\code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} is what draws this scenario.
}
\description{
The categorical counterpart of \code{\link[=estimate_significance]{estimate_significance()}}, and the reason it is
a second function rather than a branch of that one is that the two axes a
verdict is made of sit at a different granularity here. A comparison asks its
question once per feature; a contingency table is asked about as a whole, and
the place where a signed effect and a p-value exist side by side is one level
down, at the cell.
}
\details{
\code{is_signif} is three-valued and follows the rule the numeric scenarios use.
\code{NA} is undecided rather than decided against, which is what an \code{NA}
\code{std_residual} or an undefined \code{lift} leaves a cell as.

A cell holding no observation at all has a \code{lift} of exactly zero and so a
\code{log2_lift} of \code{-Inf}, an infinitely large shortfall, which clears any
magnitude cutoff. A cell whose expected count is zero has no ratio to take and
is \code{NA} in both columns. The two are different findings and are reported
differently, the same way \code{\link[=estimate_significance]{estimate_significance()}} separates a fold change of
zero from one that cannot be formed.
}
\section{Why a cell has both axes and a table has one}{

\code{\link[=compare_categorical_groups]{compare_categorical_groups()}} says that an association is not signed, which is
true of the \strong{table}: \code{cramers_v} reports how far the table sits from its null
and not in which direction, because past a 2 x 2 there is no single direction
to name. A \strong{cell} is different. It was expected at some count and observed at
another, and \code{observed / expected} says both how far it moved and which way.

That ratio is \code{lift}, the same quantity \code{\link[=simulate_categorical_groups]{simulate_categorical_groups()}} plants
and reports in \code{truth_cell$lift}, and \code{log2(lift)} is what the effect axis of
this function is. It is defined on every table, 2 x 2, 2 x 3 or larger, so one
cutoff means the same thing whatever shape the table is.

The p-value axis is \code{std_residual}, which is built to be referred to a standard
normal, so \code{2 * pnorm(-abs(std_residual))} is the cell's own two-sided
p-value. This is the standard post-hoc reading of a contingency table.

The two axes are not the same information. \code{lift} is a ratio and does not
change when the table is observed on twice as many rows; \code{std_residual} grows
with the square root of the count. So a cell can be far from what was expected
and poorly evidenced, or close to it and firmly established, exactly as
\code{log2fc} and a p-value come apart in the numeric scenarios.

A cell axis also restores a multiplicity axis. The table has as many cells as
it has, they are one family, and \code{adj_type} adjusts across them. That is why
this function computes an adjustment rather than reusing one: \code{sa_categorical}
carries no \code{pval_adj} column, there being nothing to adjust across at the level
the tests are reported at.
}

\section{The two readings}{

\describe{
\item{\code{by = "cell"}}{One row per cell of the table, with \code{log2_lift} on the
effect axis and the adjusted p-value of the cell's standardized residual on
the other. This is the reading that generalises, so it is the default.}
\item{\code{by = "table"}}{One row, the whole-table verdict: an association
measure out of \verb{$association} beside the p-value of one of the tests. There
is no multiplicity here, one table being one question, so no \code{adj_pvalue}
column is carried.}
}

Each reading ignores the arguments the other one reads, and says so rather than
letting a setting that changes nothing pass unremarked.
}

\section{Which measure a table reading reports}{

\code{measure = "auto"} reads the design, since which measures exist at all depends
on it and on the size of the table.\tabular{lll}{
   design \tab table \tab measure \cr
   independent \tab 2 x 2 \tab \code{odds_ratio} \cr
   independent \tab larger \tab \code{cramers_v} \cr
   matched, two conditions \tab 2 x 2 \tab \code{odds_ratio_paired} \cr
   matched, three or more \tab condition by response \tab \code{kendalls_w} \cr
}


Any other row of \verb{$association} can be named instead.

\code{effect_cutoff} is \code{NULL} by default, and then the verdict is the p-value
alone. The conventional thresholds for \code{cramers_v} are not defaults here
because they are conventions rather than facts about the measure, and a
default is the one place a convention is hardest to notice. Naming a number
reads it on the measure's own scale: \code{odds_ratio} and \code{odds_ratio_paired} are
ratios centred at 1, so the cutoff is a fold either way and has to be at least
1, while every other measure is centred at zero and the cutoff is compared
against the magnitude.
}

\section{Why a matched pair of conditions has no cell reading}{

A matched two-condition design is tested for symmetry, and the variance
correction the standardized residual divides by is derived for a table held
against its own margins. \verb{$cells$std_residual} is therefore \code{NA} throughout
such a result, so there is no p-value axis to read and the request is refused
rather than answered with a different quantity. \code{by = "table"} reads it, and
\code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} refuses \code{residual = "standardized"} there for the same
reason.

Three or more matched conditions are a different case. Their null is marginal
homogeneity, read off a condition-by-response table whose arithmetic is that of
independence, so the standardized residual exists and the cell reading works.
}

\examples{
smoking <- data.frame(
  smoker = rep(c("y", "n"), each = 60),
  grade  = c(rep(c("high", "mid", "low"), c(10, 20, 30)),
             rep(c("high", "mid", "low"), c(30, 20, 10)))
)
res <- compare_categorical_groups(smoking)

## One verdict per cell. The default cutoff is a doubling either way, which on
## this table only the two corners reach.
sig <- estimate_categorical_significance(res)
sig
sig$significance

## `lift` is what the effect axis is built from, and it is a ratio: the cell
## holding half of what independence expected is at 0.5.
sig$significance[c("row_level", "col_level", "lift", "log2_lift")]

## A gentler magnitude cutoff asks less of a cell than a doubling.
estimate_categorical_significance(res, log2_lift_cutoff = 0.5)

## The whole-table verdict instead. A 2 x 3 table has no odds ratio, so the
## measure `"auto"` reports is Cramer's V.
estimate_categorical_significance(res, by = "table")$significance

## Two levels of `grade` make a 2 x 2 table, which is where an odds ratio
## exists, and `effect_cutoff` is then read as a fold either way.
two_by_two <- compare_categorical_groups(
  smoking,
  category_lv = list(smoker = c("n", "y"), grade = c("low", "high"))
)
estimate_categorical_significance(two_by_two, by = "table",
                                  effect_cutoff = 2)$significance

## The cell axis against the association that was planted on it.
sim <- simulate_categorical_groups(n_samples = 400, assoc = 0.4, seed = 1)
fit <- do.call(compare_categorical_groups, sim$args)
scored <- merge(estimate_categorical_significance(fit)$significance,
                sim$truth_cell, by = c("row_level", "col_level"))
scored[c("row_level", "col_level", "lift.x", "lift.y")]

}
\references{
Haberman, S. J. (1973). The analysis of residuals in cross-classified tables.
\emph{Biometrics}, 29(1), 205-220.

Beasley, T. M. and Schumacker, R. E. (1995). Multiple regression approach to
analyzing contingency tables: Post hoc and planned comparison procedures.
\emph{The Journal of Experimental Education}, 64(1), 79-93.

Agresti, A. (2002). \emph{Categorical Data Analysis}, 2nd ed. Wiley.
}
\seealso{
\code{\link[=compare_categorical_groups]{compare_categorical_groups()}} for the input,
\code{\link[=draw_mosaic_plot]{draw_mosaic_plot()}} to draw the same residuals this reads, and
\code{\link[=estimate_significance]{estimate_significance()}} for the numeric scenarios.
}
